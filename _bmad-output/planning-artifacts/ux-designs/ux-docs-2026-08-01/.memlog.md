---
topic: SignalsAPI docs IA and page-shape conventions
updated: 2026-08-01T05:47
---

- (override) Headless run: no interactive Discovery, no creative tools, no Reviewer Gate (finalize_reviewers empty and subagents forbidden this session). Fast path, spines authored directly from PRD + brief + project-context + brainstorm memlog.
- (decision) DESIGN.md is a freeze contract, not a design exploration: all 14 colors, 4 radii, 4 shadows, 2 font families transcribed verbatim from _sass/color_schemes/signalsapi.scss, _sass/custom/custom.scss and _includes/head_custom.html. Reason: static docs site with an existing identity; the overhaul changes content shape, not appearance.
- (decision) IA restructure ships ZERO redirects. just-the-docs parent: resolves by page title, not directory, so nesting is a front-matter edit and no file moves. Reason: the URL surface is the only SEO asset and scope item 15 forbids unpaired deletion.
- (decision) Nav tree = 11 top-level entries with contiguous integer nav_order 1..11 at every level. Kills F5 (duplicate 6, unused 5) and the float shims (1.5, 2.5, 3.5-3.7, 11.1-11.3) permanently via a check.rb sibling-group assertion rather than by convention.
- (decision) APIs promoted to a top-level section by front matter only (memlog L118/L284): apis/index.md is a new router, the five existing API pages keep their features/ URLs and gain parent: APIs. Rejected git mv into apis/ because it costs five redirect_from entries for a cosmetic gain.
- (decision) Five page_type values (router, concept, task, symptom, reference), each with a front-matter contract and a required H2 skeleton, each stated with its check.rb or Vale assertion. A convention that cannot be asserted was excluded from the document.
- (decision) page_type: task requires an '## If it didn't' H2 linking a /troubleshooting/ page. This is the assertion that prevents the Priya flow step-4 dead end (empty result set with no named recovery path).
- (decision) Four pages leave the nav for a new _includes/footer_custom.html with nav_exclude: true (memlog L98): request-new-feature (today nav_order 0, the literal first item), is-it-working, privacy-policy, tos.
- (assumption) Troubleshooting page set (7 symptom pages) inferred from memlog L72/L94/L114 plus the audit findings; every one is a re-shaping of a fact already present in-repo as a feature-page aside, not new research. Titles are provisional.
- (assumption) Concepts children (anatomy-of-a-lead L115, pipeline L82, glossary L99) and the three role-shaped quickstarts are taken from the PRD FR set; how-it-works.md is nested under Concepts by front matter with no file move.
- (event) Spines finalized: DESIGN.md and EXPERIENCE.md written, status final. No mocks, no wireframes, no imports - static docs site, no screens to render. Four open questions recorded in EXPERIENCE.md.
