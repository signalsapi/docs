---
topic: Criticize the signalsapi docs site and generate ways to make it better
goal: Everything at once: deflect support tickets, raise conversion, win SEO/organic traffic, and make onboarding self-serve
mode: autonomous
status: complete
updated: 2026-07-31T22:20
---

- (insight by coach) Audit run: 14 agents, 6 lenses, 84 findings raised, 81 survived adversarial verification, 3 refuted
- (insight by coach) Root problem: the site is a marketing brochure wearing a docs theme - 1510 words across the first 4 pages contain zero links into the documentation
- (insight by coach) No self-serve path exists: the only CTA on the whole site is booking a sales call at signals.fillout.com/meet
- (insight by coach) The product's primary action - creating a search - is documented nowhere except a parenthetical inside website-monitoring.md
- (insight by coach) Object model (Search/Filter/Persona/Lead list/project) is called 4-5 different names and defined on zero pages
- (insight by coach) docs/ directory is completely empty; features/ is a 20-page junk drawer behind a 10-word landing page
- (insight by coach) SEO broken at the root: url has a trailing slash so all 28 canonicals emit a double slash; no sitemap, no robots.txt, no 404, no og:image, identical meta description on every page
- (insight by coach) Unedited just-the-docs template README is live at docs.signalsapi.com/README.md telling visitors to click use this template; push.sh and run.sh also served publicly
- (insight by coach) Pricing contradiction: faq.md sells phone numbers at 49-199 GBP/month while find-phone-numbers.md says phones come from the customer's own LeadMagic key and cost SignalsAPI nothing
- (insight by coach) Credits are the billing unit, visible in the app header, gating every lookup - and documented on zero pages except one changelog line
- (insight by coach) Trust gaps: privacy policy 18 months stale and never mentions the third-party personal data the product sells; 22 affiliate links carry no rel=sponsored and no disclosure
- (insight by coach) Changelog dead since 2025-04-09 (16 months) yet is the only record of several shipped features like Recruit CRM; 729 lines, 53 un-lazy PNGs, 1.9MB
- (decision by coach) Technique batch chosen: Job to Be Done, How Might We, Assumption Reversal, Five Whys, SCAMPER, Worst Possible Idea, Trait Transfer, Kill the Crown Jewel, Relativity Frame Shift, Trickster's Gambit, Post-Scarcity Test, Backcasting
- (technique by coach) started Job to Be Done
- (idea by coach) "Why is my lead list empty?" — nine causes, one page — New top-level page /troubleshooting/empty-leads.md structured as a symptom decision tree covering the nine real causes: no provider key (free tier, bring-your-own-people-provider.md:14), key status invalid on Settings → Provider, phones requiring LeadMagic (find-phone-numbers.md:12), the <70% email-confidence drop (faq.md:61), credits at zero or 60-day-expired (whats-new/index.md:53), run cadence by plan (whats-new:612), "Manual Approval" holding leads unuploaded (integrations.md:34-36), dedup mode "Among all lists", and the DNC list. Each branch names the exact screen to check and the exact string you should see there. [support-deflection/M/safe]
- (idea by coach) Ship a status-string dictionary for every label the app shows — Page /reference/statuses.md listing every literal string a user can see and what to do about it: Searching / Leads found / Qualified / Enriching… (documented only at whats-new/index.md:15-21), provider key active vs invalid, lead status Approved vs Uploaded (integrations.md:38), the email confidence question-mark badge, and the API's own email_status: verified, is_usable, unlocked_at: null (api-access.md:109-112). [support-deflection/M/safe]
- (idea by coach) Cost calculator: your credits plus your provider bill — Interactive page /pricing/calculator with client-side JS in the Jekyll build. Inputs: companies per run, runs per week, people per company, which of the eight providers is connected, phone lookup on/off. Outputs: SignalsAPI credits consumed and an estimated provider bill in £ — computed from the at-source vs after-fetch matrix already published at compare-people-data-providers.md:51-60, since after-fetch filters are people the provider already charged for. [conversion/L/stretch]
- (idea by coach) A self-serve vendor pack that closes procurement in one sitting — New /trust/ section: a downloadable DPA, a sub-processor table naming all eight people-data providers plus hosting and the AI model vendor, a lawful-basis statement for processing decision-maker personal data, a key-handling page promoting bring-your-own-people-provider.md:89-92 into a real commitment (encrypted at rest, masked last-4 fingerprint, never logged), breach-notification window, deletion SLA, and a pre-filled CAIQ-lite questionnaire as a downloadable file. Re-date privacy-policy.md off "January 22, 2025". [trust/L/stretch]
- (idea by coach) Publish a public demo key for Tier 0 plane reads — A /try page publishing a rate-limited, read-only demo key and base URL that serves exactly two Tier 0 cached reads — POST /v1/clay/enrich and GET /v1/reqs/search — so any prospect can curl their own client's domain and get back is_hiring, open_req_count, new_roles_30d, velocity and is_surge before ever emailing anyone. Directly replaces agent-data-plane.md:113 "not yet open for self-serve signup, and there is no public base URL". [conversion/L/wild]
- (idea by coach) Serve llms.txt, OpenAPI and an MCP manifest from the docs domain — Emit three static files from Jekyll at docs.signalsapi.com: /llms.txt indexing every page with a one-line purpose; /openapi.yaml covering both surfaces (api.signalsapi.com's three endpoints with Bearer auth, and the plane's /v1 with X-API-Key); and /.well-known/mcp.json declaring the eight tools listed at agent-data-plane-mcp.md:43-50 with their typed arguments. Add the missing sitemap.xml and robots.txt in the same change. [seo/M/stretch]
- (idea by coach) Rename Features into five sections titled as reader sentences — Replace the 20-child features/ junk drawer with five sections named for the job, not the noun: "Get my first leads", "Stop getting the wrong leads" (advanced-search, location-based-filtering, work-arrangement-filter, DNC, dedup), "Get leads into my CRM" (integrations), "Spend less on people data" (the two provider pages, find-phone-numbers), "Build on the data" (api-access + the four plane pages nested under it). Each landing page opens with the literal JTBD sentence, not a feature inventory. [onboarding/M/stretch]
- (idea by coach) One /clay hub that routes push versus pull in two rows — New /clay page that owns the term: a two-row decision table — "I have a list of domains and want hiring data back" → the enrichment column at agent-data-plane-clay.md; "I want SignalsAPI to fill my Clay table with new leads automatically" → the webhook push at integrations.md:40 — plus a third row for running both (push leads in, enrich them back). Retitle the two existing pages "Clay — push leads" and "Clay — enrich companies" so lunr surfaces the hub first. [support-deflection/S/safe]
- (idea by coach) Substantiate "290+ sources" with a real coverage catalogue — New /sources page listing the ATS and job-board families observed (the boards field at agent-data-plane-clay.md:89 already returns greenhouse), coverage broken out by country, refresh cadence per family, and a "request a source" form. Link it from index.md:17 and faq.md:11 — the two places the unsupported 290+ claim currently appears. [seo/M/stretch]
- (idea by coach) Turn the dead changelog into a proof-of-life instrument — Split whats-new/index.md (729 lines, 53 screenshots, 1.9 MB, last entry 18.06.2026) into year pages, add a JSON/RSS feed, put a "last shipped: N days ago" line at the top, make Read more links mandatory on every feature entry, and backfill the two 2026-07-09 launches — API access and the agent data plane — that shipped with docs and zero announcement. [trust/M/stretch]
- (idea by coach) "Not enough leads? Pull these seven levers, in order" — A ranked page of real controls with expected effect on volume: switch keyword to semantic search (advanced-search.md:26), drop Job title required words, raise Maximum age of job posting from 7 to 30, turn off Hard to fill (30+ days or 3+ reposts) and Hiring surge, widen Location patterns beyond one city, switch to People Data Labs so more people survive at-source filtering per credit, and add a second search. Each lever states the trade-off it costs in precision. [support-deflection/S/safe]
- (idea by coach) Define a credit as a ledger, with three worked monthly scenarios — A /credits page that publishes both metering systems side by side: app credits (what consumes one, plan allowance, the 60-day expiry buried at whats-new/index.md:53, free-tier limit of 150) and the plane's four meter units call / change / watch / forced_fresh from agent-data-plane.md:100, including the rule that the change feed bills per event returned rather than per poll. Then three worked months for real agency shapes. [conversion/M/stretch]
- (idea by coach) Publish the affiliate commissions, then name the pick that earns nothing — On compare-people-data-providers.md and bring-your-own-people-provider.md, add rel="sponsored nofollow" to all 14 ?via=/?fp_ref= links, publish the actual commission per provider in a disclosure table, and add an "if we earned nothing we'd pick People Data Labs (no affiliate link — it filters title, country, city and skills at source, so it wastes the fewest of your credits)" line. [trust/S/wild]
- (idea by coach) "Your first 60 minutes" — a persisted onboarding checklist — One page with a localStorage-persisted checklist in the real order: connect a provider at Settings → Provider, create a search via Search → Add Search, set the ICP filter, build a persona, add a destination at Settings → Integrations → Add Integration, run, download the CSV. Every step declares its prerequisite and the exact symptom if you skip it ("skip step 1 and step 5 returns companies with no emails"). [onboarding/M/safe]
- (idea by coach) Publish the founder's inbox as docs, in the customer's words — New /answered/ section: take the last 50 threads sent to mykola@signalsapi.com, anonymise, and publish each as a short page using the customer's original question wording as the H1 — "why does the phone column stay empty", "can I run two providers at once", "my Bullhorn export stopped" — with the real reply as the body and a link to the feature page that owns the detail. [support-deflection/M/wild]
- (idea by coach) Text-first rule, with CI that fails on stale screenshots — Adopt the rule that every instruction must be executable with images off, then rewrite the pages that violate it — find-decision-makers.md and remove-duplicate-signals.md currently carry their entire payload in alt="" screenshots from 2024-10. Demote screenshots into a collapsed "What this looks like" block, and add a just check-screenshots CI recipe that fails the build when an image file is older than the last content edit of the page embedding it. [content-ops/M/stretch]
- (idea by coach) Four honest "vs" pages, each with a where-we-lose section — Comparison pages against PredictLeads, TheirStack, UserGems/Champify, and the DIY option ("just scrape Greenhouse yourself"). Each covers the same axes — source breadth, event granularity (the opened/reposted/reobserved ledger at agent-data-plane.md:32-36), whether people data is bundled or BYO-key, MCP/agent access, and price shape — and each ends with an explicit "choose them instead when…" section. [seo/L/stretch]
- (idea by coach) AI-variables page becomes a provider-aware prompt playground — Rebuild features/ai-variables.md as a table (description, example value, provider prerequisite) plus a client-side widget: paste a prompt template, pick one of the eight providers, and it renders the filled example while greying out variables that return null for that provider — {phone} and {phone_status} blank for everything but LeadMagic, {headline} blank for everything but Icypeas, People Data Labs and Prospeo. Add the missing ai_field_1–5 that integrations.md:77 already ships. [onboarding/M/stretch]
- (idea by coach) A printable one-pager the champion forwards to the budget-holder — New /for-decision-makers page, print-styled with a PDF download, answering exactly three objections on one sheet: total monthly cost including the third-party provider half, the GDPR position with a link to the new DPA and sub-processor list, and an honest answer to "does this replace my BDR" (it replaces sourcing and timing, not the conversation). Written in plain register — no "which side do you want to be on in 12 months". [conversion/S/stretch]
- (idea by coach) Support page that pre-empts the biggest false-alarm ticket — Retire is-it-working.md (23 words) into one /support page: a single address (support@, replacing the mykola@ / support@ split across nine pages), stated hours, a response target, and the status.signalsapi.com link — plus a "what 'working' means" section explaining that Tier 0 reads are deliberately freshness-floored (agent-data-plane.md:82-85) and that data withheld until it ages past the floor is by design, not an outage. [trust/S/safe]
- (idea by coach) Docs that run: live plane reads embedded in the reference pages — On agent-data-plane.md and agent-data-plane-clay.md, replace the static zollsoft.de JSON samples with a domain input that calls the demo key against the real endpoint and renders the actual response inline, headers included — X-Data-Freshness and X-Meter-Class beside the body so the metering model is demonstrated rather than described. Depends on the public demo key. [developer-experience/L/wild]
- (idea by coach) A glossary in _data that CI enforces against undefined jargon — Create _data/glossary.yml defining signal, search, filter, persona, lead list, project, credit, run, unlock, usable lead, as_of, reposted, and personation — then an include that turns the first occurrence on any page into a hover-defined link, plus a build step that fails when a page uses a listed term before it is defined or introduces a jargon term absent from the file. Settles project-vs-lead-list once, in one place. [content-ops/L/stretch]
- (technique by coach) started How Might We
- (idea by coach) "Why is my leads list empty?" — a branching diagnostic, not an FAQ — New page features/no-leads-troubleshooting.md built as a client-side decision tree whose first question is the badge the user is staring at (Leads found / Qualified / Searching...). Branches terminate at the exact screen and cause: no key at Settings → Provider (free tier, bring-your-own-people-provider.md:14), persona title list matched nobody, "Email is required" dropped everyone, stop words from advanced-search.md:44 too broad, Maximum age of job posting too tight. Every leaf links the owning page and states what it costs to retry. [support-deflection/M/safe]
- (idea by coach) Reference page for every literal string the app puts on screen — New features/statuses-and-badges.md documenting each exact string a user can read and then paste into the docs search: Enriching..., Leads found, Qualified, Searching..., "available on paid plans", provider status active/invalid, the em-dash in the Phone column, and the plane's 202 {"status":"crawling"}. Each row: what it means, what caused it, what to do, which page owns it. [support-deflection/S/safe]
- (idea by coach) One field dictionary: CSV ↔ Clay payload ↔ REST JSON ↔ AI variable — New features/field-reference.md with one row per concept and five columns: CSV column (decision_maker_phone), Clay webhook field (linkedin_url, ai_field_1–5 from integrations.md:54-77), REST JSON path (person.linkedin_url from api-access.md), AI variable ({phone}, {phone_status}), and Requires (free / any provider / LeadMagic only). Replaces ai-variables.md's 65-word bare list. [support-deflection/M/safe]
- (idea by coach) Vocabulary as data: _data/glossary.yml plus an inline term include — Define Signal, Search, Filter, Persona, Lead list, project, credit, personation once in _data/glossary.yml; render /glossary from it; add {% include term.html t="persona" %} that emits a dotted-underline tooltip. Retire "personation" (find-phone-numbers.md:24,72) to the literal button name, and state project ≡ lead list once, in data, not prose. [onboarding/M/stretch]
- (idea by coach) "What do I need for this?" — one requirements matrix, CI-enforced — _data/prereqs.yml maps every feature page to free-tier / any-provider-key / LeadMagic-only / paid-plan / integration-tier (e.g. Recruit CRM needs Business tier per whats-new/index.md:25). It renders both a Requires badge at the top of each page and a single matrix page features/what-do-i-need.md. CI fails if a page in features/ has no entry. [onboarding/M/stretch]
- (idea by coach) Provider bill estimator that turns affiliate links into honest advice — Interactive block on features/compare-people-data-providers.md: enter signals/month, people per company, and target countries; it uses the page's own at-source vs after-fetch matrix to estimate paid lookups per provider and shows the waste delta (People Data Labs filters title+country+city+skills at source; Hunter and Tomba pull the whole department and match titles after fetch). Output ends in a disclosed affiliate link plus a "have us run it for you" option. [conversion/L/stretch]
- (idea by coach) Enumerate all 290 sources — stop asserting the number — New /sources page listing every monitored job board, ATS, funding database and filings source with country and category, plus a client-side filter box. Seed from whats-new/index.md:48 (TechInAsia, Reed, Justjoin, Jooble, Finn.no, Jobindex, Monster, RocketJobs) and :57 (HiringCafe). Generated from _data/sources.yml so the changelog and the page can't disagree. [seo/L/stretch]
- (idea by coach) Publish quarterly hiring-motion reports off the events ledger — A /reports section rendering data only we hold: repost-pain and role-demand cuts from the append-only hiring-events ledger described in agent-data-plane.md:24-51 — e.g. "UK construction PM roles reposted 3+ times, QoQ" or "ATS migrations off Taleo, 12 months". Each report ends with the exact advanced-search config that reproduces it, and the /v1 call that produced it. [seo/L/wild]
- (idea by coach) Ship the docs as an MCP server before the plane is hosted — Publish /llms.txt and /llms-full.txt (the full corpus as one file), then host signalsapi-docs, an MCP server whose tools search and fetch these pages. Link it from agent-data-plane-mcp.md, which currently says the real server is "code-complete but not yet deployed". [developer-experience/M/wild]
- (idea by coach) Sandbox key and demo ledger — let strangers call /v1 today — Stand up a read-only sandbox base URL over ~20 frozen public companies, issue keys instantly from a form on features/agent-data-plane.md, and make every curl on agent-data-plane-api.md runnable against it. Replace "Pricing is not yet published" with indicative Tier 0 / Tier 1 numbers and stated rate limits, labelled indicative. [conversion/L/wild]
- (idea by coach) Ship openapi.yaml for api.signalsapi.com and generate the reference from it — Author an OpenAPI 3.1 spec covering GET /projects/, GET /projects/{id}/leads, and the export endpoint, publish it at /openapi.yaml, generate the tables in features/api-access.md from it, and add a copy-paste Postman/Insomnia import plus a jq one-liner per example. [developer-experience/M/stretch]
- (idea by coach) Changelog becomes a collection that writes the feature pages — Split whats-new/index.md into a _changelog/ collection, one file per date, with a features: [advanced-search, integrations] tag. Feature pages render "Recent changes to this feature" from the tag; the changelog gets an RSS/Atom feed; CI fails on any entry with no feature tag. Backfill Recruit CRM into integrations.md as part of the migration. [content-ops/L/stretch]
- (idea by coach) Publish our own doc rot: a /docs-health page with per-page verified dates — Add verified_against and verified_on to front matter, render "Verified against the app on <date>" in the page footer, and publish /docs-health — a public table of all 28 pages sorted by staleness, red past 90 days. Ships red on day one for the eight 2024-10 GitBook-era pages. [trust/M/wild]
- (idea by coach) Screenshots as build artifacts: Playwright capture against a demo tenant — Add a Playwright spec that logs into a seeded demo account and re-captures all 17 feature PNGs (Settings → Provider, Persona with Find phone numbers, the Filters screen, the leads list with the Phone column) at fixed viewport, writing straight into features/. Run it on a schedule; CI fails when a screenshot predates the current app release. [content-ops/L/stretch]
- (idea by coach) Capture every zero-result docs search, with no backend — Hook just-the-docs' lunr search: when a query returns zero results, show "We don't have a page for '<query>' yet — tell us" with a prefilled mailto/Formspree containing the query and the referring page. Review weekly; each recurring query becomes a heading in statuses-and-badges.md or the FAQ. [content-ops/S/safe]
- (idea by coach) Docs diet: delete four pages, the pitch, and 1.9 MB of PNGs — Delete is-it-working.md (23 words duplicating faq.md:77), nav_exclude request-new-feature.md out of the nav_order 0 slot, cut index.md's closing pitch ("Which side do you want to be on in 12 months?" plus the consultation CTA) down to three sentences and next-step links, and drop or re-encode the 53 changelog screenshots at 2x display width. [conversion/S/wild]
- (idea by coach) Task recipes named after the buyer's desk, not our features — New /recipes section with 5-6 end-to-end playbooks: "Fill a construction PM desk in Manchester" (semantic search + location patterns + Exclude staffing agencies + Hard to fill + Persona + Bullhorn destination), "Catch first-ever RevOps hires in DACH", "Get mobile numbers into Recruit CRM". Each is a single page with the full config and expected output. [seo/L/stretch]
- (idea by coach) Two-question Clay router at the top of both Clay pages — Add an identical block to features/integrations.md#integrating-with-clay and features/agent-data-plane-clay.md: "Want leads pushed INTO a Clay table? → this page. Want to enrich companies you already have in Clay? → that page." Retitle them "Clay — push leads" and "Clay — enrich companies", and cross-link them explicitly. [support-deflection/S/safe]
- (idea by coach) Make the affiliate links the trust moment instead of the liability — Add rel="sponsored nofollow" to all 22 ?via=/?fp_ref= links and a plain-English box on both provider pages: "We earn a referral fee if you sign up through these links. SignalsAPI charges no credits for people lookups — we make nothing on your enrichment spend. If you'd rather not think about it, we can run enrichment for you at <price>." [trust/S/safe]
- (idea by coach) Per-page descriptions plus FAQPage and HowTo JSON-LD from front matter — Add a one-sentence description: to each page's front matter (start with api-access, agent-data-plane, compare-people-data-providers, integrations, advanced-search, find-phone-numbers) and extend _includes/head_custom.html to emit FAQPage JSON-LD generated from faq.md's H2 questions and HowTo JSON-LD from pages whose H2 is a numbered setup section. [seo/M/safe]
- (idea by coach) A 404 that has already run the search for the reader — Add 404.md with permalink: /404.html that parses the mistyped path, runs it through the existing lunr index client-side, and renders ranked results plus links to Getting started and the app — then add jekyll-redirect-from mappings for GitBook-era URLs, .md-suffixed paths, and any /features/* slug that moves during the IA split. [seo/M/stretch]
- (idea by coach) CI that fails when the docs lie about the product — Extend .github/workflows/ci.yml past its single jekyll build step: html-proofer for links, then assertions — no occurrence of "personation" or "100% reliable", no mykola@ in customer-facing prose, every ?via=/?fp_ref= carries rel="sponsored", every features/*.md has description: and a _data/prereqs.yml entry, provider tables generated from _data/providers.yml, no page missing verified_on. [content-ops/M/stretch]
- (idea by coach) Search sandbox: test stop words and required words before spending credits — An interactive page features/search-tester.md where a user pastes a real job title and description, enters their Search for / Job title required words / Job description required words / Stop words, and sees instantly whether the signal is kept or dropped and which rule decided — implementing the OR-within-field, AND-across-fields logic documented at advanced-search.md:42-46 client-side. [onboarding/M/wild]
- (idea by coach) Procurement pack: /security with a generated sub-processor table — New /security page covering what we process about data subjects we never contracted with, lawful basis, retention, deletion, encryption of customer provider credentials, and a sub-processor table generated from the same _data/providers.yml that renders the compare tables — plus a DPA request link. Re-date privacy-policy.md (still "Last Updated: January 22, 2025") to match. [trust/M/stretch]
- (technique by coach) started Assumption Reversal
- (idea by coach) Search simulator on advanced-search.md: paste a job ad, see keep/drop — Add a JS widget to features/advanced-search.md that runs the exact matching logic that page documents. User pastes a job title + description, fills the same fields (Search for, Job title required words, Job description required words, Stop words, Required/Exclude skills, Exclude staffing agencies, Max age), and the page returns KEPT or DROPPED with the specific rule that fired. No network call — the AND-across-fields / OR-within-field logic is already fully specified in prose at lines 42-46. [support-deflection/M/stretch]
- (idea by coach) Symptom pages the app deep-links to from its own empty states — Create /troubleshooting with one page per observed failure state, titled as the user experiences it: "My leads list is empty", "My phone column is blank", "My credits hit zero overnight", "Lead says Approved but nothing reached my CRM", "My search runs but finds nothing new". Each is a decision tree ending at a named cause (no provider key → free tier, provider isn't LeadMagic, Manual Approval enabled, dedup set to "Among all lists: Yes"). Then have app.signalsapi.com link each empty state directly to its anchor. [support-deflection/M/stretch]
- (idea by coach) Provider switcher that rewrites every page to your connected provider — A persistent "Your provider: [LeadMagic ▾]" control (localStorage, set once) that conditionally renders content across features/compare-people-data-providers.md, find-phone-numbers.md, find-decision-makers.md and ai-variables.md. Pick Hunter and {phone}/{headline} in the AI variables list grey out with "not returned by Hunter"; the phone page shows "requires LeadMagic — you are on Hunter"; the filter table collapses to your provider's row with at-source vs after-fetch spend warnings. [support-deflection/M/stretch]
- (idea by coach) _data/providers.yml as the only place a provider fact exists — Move all eight providers into _data/providers.yml with fields: name, url, affiliate (bool), email/mobile/linkedin/headline capability, per-filter placement (title/country/city/skills/department/seniority = at-source|after-fetch|unsupported), credential shape, department and seniority enums. Render both tables in compare-people-data-providers.md, the setup list in bring-your-own-people-provider.md, the prerequisite column in ai-variables.md and the "LeadMagic only" claim in find-phone-numbers.md from that one file via Liquid — which also lets the loop emit rel="sponsored nofollow" on every affiliate URL automatically. [content-ops/M/safe]
- (idea by coach) Changelog becomes _data/changelog.yml with three rendered views — Convert the 105 entries in whats-new/index.md into _data/changelog.yml (date, title, area, body, image, alt, page). Render: (1) the changelog page, paginated by quarter with loading="lazy" and height attributes; (2) an auto-injected "What changed here" block on each feature page whose slug matches an entry's `page` key; (3) /whats-new/feed.xml and /whats-new.json. Backfill 2026-07 entries for API access and the agent data plane. [content-ops/L/stretch]
- (idea by coach) Ship /llms.txt and raw .md routes — docs an agent can read — Generate /llms.txt (page index with one-line descriptions) and /llms-full.txt (whole corpus concatenated) from the Jekyll page list at build time, and serve each page's source markdown at /features/agent-data-plane-api.md.txt. Add both to robots.txt and to a new sitemap. [developer-experience/S/safe]
- (idea by coach) OpenAPI 3.1 spec becomes the source; the API pages become views — Author the agent data plane surface once as /openapi.json (all /v1 routes, the provenance envelope schema, X-Data-Freshness and X-Meter-Class headers, the 202 crawling response, the four meter units). Generate the endpoint sections of features/agent-data-plane-api.md AND the eight-row tool table in agent-data-plane-mcp.md from it, with the MCP page's REST-equivalent column derived rather than typed. [developer-experience/L/stretch]
- (idea by coach) Ship signalsapi-docs as an MCP server before the plane's MCP exists — Publish a small MCP server that exposes the docs corpus as agent tools — search_docs(query), get_page(slug), list_endpoints() — hosted at a public URL with a connect snippet on features/agent-data-plane-mcp.md. No API key, no tenancy, no metering: it reads the same _site build the website serves. [developer-experience/M/wild]
- (idea by coach) Credits calculator that prices your provider bill, not just ours — A /pricing page whose centre is a calculator: searches per day × companies per run × decision-makers per company × your provider's per-lookup price → monthly credits consumed, credits expiring under the 60-day rule, and your separate provider spend. Model the at-source vs after-fetch distinction from compare-people-data-providers.md so choosing Hunter over People Data Labs visibly raises the bill. Publish plan allowances and the free tier's 150 credits alongside it. [conversion/M/stretch]
- (idea by coach) Free domain lookup on the docs site: type a domain, get hiring pulse — Put a public, no-auth input on features/agent-data-plane.md and on index.md: enter a company domain, get back is-hiring, open req count, repost pain and the hiring-pulse direction — the Tier 0 cached reads the page already describes as free. Rate-limit by IP, cap at a few lookups, then issue a self-serve key with a base URL instead of routing to mykola@signalsapi.com. [conversion/L/wild]
- (idea by coach) Generate "Who is hiring [role] in [city]" pages from the ledger — Use the role-demand and repost-pain primitives to build a programmatic section: /hiring/{role}/{geo} pages showing current open requisition counts, 90-day demand trend, the companies reposting that role most often, and typical time-to-fill. Seed with the top ~300 role×geo pairs the ledger actually covers, refresh weekly from the same data the plane serves, and put a search CTA on each. [seo/L/wild]
- (idea by coach) "You are here" pipeline strip on every feature page — Add a `stage:` front-matter key (search | filter | persona | leadlist | delivery | api) to each page under features/, and render a compact Search → Filter → Persona → Lead list → Delivery strip at the top via an include, current stage highlighted, previous stage linked. location-based-filtering.md, which names all three objects in three bullets and closes with "Simple as that.", becomes self-orienting; find-decision-makers.md's strip links back to bring-your-own-people-provider. [onboarding/S/safe]
- (idea by coach) A trust page made of measured numbers, not adjectives — Create /trust: the eight people-data providers listed as named sub-processors with what each receives, the customer-supplied API key storage model, a DPA request link, 90-day uptime pulled from status.signalsapi.com, the measured email verification rate replacing faq.md:65's "100% reliable", and the actual median signal latency replacing "real-time". Link it from bring-your-own-people-provider.md and the footer. [trust/M/stretch]
- (idea by coach) verified_on and owner front matter, rendered as a freshness chip — Add `verified_on:` and `owner:` to every page's front matter, render "Verified 2026-07-31" in the page header, and add a CI step that fails when a page under features/ is >180 days unverified or when a page's app.signalsapi.com deep links 404. privacy-policy.md, faq.md and the eight GitBook-era screenshot pages flag on day one. [content-ops/S/safe]
- (idea by coach) Screenshots become CI artifacts: a stale image fails the build — Declare screenshots in front matter (`screenshots: [{selector, route, alt}]`) and add a scheduled Playwright job that logs into a demo account, navigates each declared route, re-shoots at the declared crop, and fails the build if a selector no longer exists. Retire the 8 hand-captured 2024-10 PNGs on find-decision-makers.md, remove-duplicate-signals.md, do-not-contact-list.md, filter-leads-with-ai.md and personalize-emails-with-ai.md into that pipeline; alt text comes from the declaration, so 62 empty alts get filled by construction. [content-ops/L/wild]
- (idea by coach) Replace screenshot-only instructions with field reference tables — For each screenshot that carries meaning, write the control out in text: remove-duplicate-signals.md gets a table for "Companies can be matched again" with the real options and defaults (Within this list: Never / After 1 month / …; Among all lists: Yes / No) and gets renamed to match the control it actually documents, not job dedup. Same treatment for the Persona settings on find-phone-numbers.md and the Do-not-contact list controls. [trust/S/safe]
- (idea by coach) Search aliases in front matter so the app's four nouns resolve to one page — Add a `search_aliases:` front-matter list feeding the lunr index: the Core-concepts page claims project, lead list, leadlist, persona, personation, filter settings, search settings; agent-data-plane-clay.md claims "Clay enrichment column", "Clay HTTP provider"; integrations.md claims "Clay webhook", "push to Clay". Prefix the two Clay result titles "Clay — enrich companies" and "Clay — push leads". [support-deflection/S/safe]
- (idea by coach) Surface router on index.md: six questions, one correct entry point — Replace the closing pitch on index.md with a short branching router: Do you want leads in the app (→ create your first search) · leads pulled into your own code (→ API access) · hiring history for an AI agent (→ agent data plane REST) · agent tools (→ MCP) · SignalsAPI as a Clay enrichment column (→ agent-data-plane-clay) · leads pushed into a Clay table (→ integrations#clay) · anything else (→ webhook). Each branch names the surface and its price/access state honestly. [conversion/S/stretch]
- (idea by coach) Turn the 404 into a GitBook legacy redirect router — Add 404.md with `permalink: /404.html` that runs a client-side lookup of the attempted path against a _data/redirects.yml map of old GitBook slugs and old .md-suffixed paths (including the still-live features/filter-leads-with-ai.md link target ai-variables.md), redirects on a hit, and on a miss shows the sidebar plus the search box pre-filled with the attempted slug and the five symptom pages. [onboarding/S/safe]
- (idea by coach) Write the switching guides aimed at Clay and Apollo users — Three comparison pages targeting the queries competitors' customers type: "Replace your Clay job-postings enrichment column with SignalsAPI" (field-by-field mapping to the agent-data-plane-clay columns), "SignalsAPI vs buying a prospect list", and "Hiring signals vs scraping job boards" (built on the opened/reposted/reobserved distinction no scraper produces). Each ends at the domain lookup or a search setup, not a calendar link. [seo/M/stretch]
- (idea by coach) Transcript-first Zoho and Firefish pages instead of bare YouTube links — Replace integrations.md's two lines — a bare youtu.be/xRhsO_mXxFM for Zoho and youtu.be/CTC4VHMeYqw for Firefish — with written numbered setup steps derived from the videos (credentials needed, where to find them, field mapping, the Destination selection), with the video embedded below as reinforcement. While there, add the missing Recruit CRM, Instantly and Webhook sections, finish the sentence truncated at line 24, and reconcile the numbered list against the sections that actually exist. [seo/S/safe]
- (technique by coach) started Five Whys
- (idea by coach) Restore quick-start.md from commit 835852a — as text, not video — Recreate the deleted /quick-start.md (git show 835852a^:quick-start.md) as a written 10-minute path: register at app.signalsapi.com/register → Search → Add Search → keywords + location → save → wait for first run → open the lead list. No embed. nav_order 2, directly under Introduction. [onboarding/S/safe]
- (idea by coach) "Why is my leads list empty?" — the four-branch diagnostic page — One troubleshooting page keyed to the four real failure modes buried as asides today: no provider key → free tier, company signals only (bring-your-own-people-provider.md:14); phone column blank → LeadMagic is the only mobile provider (find-phone-numbers.md:12); filters too narrow; run cadence set by plan (whats-new/index.md:612). Each branch states the symptom the user actually sees, then the fix. [support-deflection/S/safe]
- (idea by coach) Clay chooser banner: push-leads vs enrich-companies, on both pages — Add a shared _includes/clay-chooser.html two-row table injected at the top of both integrations.md#integrating-with-clay and features/agent-data-plane-clay.md: "Want SignalsAPI to push new leads into a Clay table? → webhook setup. Want Clay to look up a company you already have? → enrichment column." Retitle them "Clay — push leads" and "Clay — enrich companies". [support-deflection/S/safe]
- (idea by coach) Strip index.md to a six-link router; move the pitch to signalsapi.com — Cut index.md's closing pitch, the Virtual Networker cross-sell and the "which side do you want to be on in 12 months" line. Keep three sentences of what SignalsAPI is, then six tiles: Quick start, Core concepts, Advanced search, Bring your own people-data provider, Integrations, API access. One "Start free" button to app.signalsapi.com/register. [onboarding/S/safe]
- (idea by coach) CI check: currency symbols outlawed outside pricing.md — Add a grep-based step to .github/workflows/ci.yml that fails the build if £, €, $ or the word "per month" appears in any .md other than pricing.md and tos.md. Ships alongside a new /pricing page carrying plans, free-tier 150 credits, 60-day credit expiry and the BYO-provider phone model. [trust/S/safe]
- (idea by coach) Footer include: banish request-new-feature and is-it-working from nav — Add _includes/footer_custom.html holding status.signalsapi.com, support, privacy-policy, tos and the Featurebase board; set nav_exclude:true on request-new-feature.md (currently nav_order 0 — the literal first item under Features), is-it-working.md, privacy-policy.md and tos.md. [conversion/S/safe]
- (idea by coach) _data/glossary.yml as the vocabulary lock, enforced by a term linter — One YAML defining Signal, Search, Filter, Persona, Lead list (= `project` in the REST API), Credit and the pipeline order. It generates /concepts and feeds a CI linter that fails on banned strings: "personation" anywhere, "project" outside api-access.md, "lead list" inside api-access.md, "person settings" (location-based-filtering.md:18's invented fifth name). [support-deflection/M/stretch]
- (idea by coach) Changelog becomes _data/releases.yml with a 45-day staleness budget — Convert whats-new/index.md's 105 hand-maintained entries into _data/releases.yml (date, title, body, feature_page, image), rendered by a Liquid template. Add a CI step that fails when the newest entry is older than 45 days. Backfill 2026-06-30 API access and 2026-07-09 agent data plane, both of which shipped with docs and no announcement. [content-ops/M/stretch]
- (idea by coach) Tag every app link ?ref=docs so activation becomes a visible number — Append ?ref=docs&page=<slug> to all app.signalsapi.com links via a Liquid filter, add privacy-friendly analytics to _includes/head_custom.html, and publish a weekly activation count next to the Fillout meeting count. [conversion/S/stretch]
- (idea by coach) Mermaid pipeline diagram replaces how-it-works.md's passive bullet list — Rewrite how-it-works.md (nav_order 4 → 2) as an active-voice diagram: 290+ sources → Signal → Search → Filter (ICP) → Persona (decision-makers, requires a provider key) → Lead list → Integration destination. Every node hyperlinks to the page that owns it; the Persona node carries the provider-key prerequisite inline. [onboarding/M/stretch]
- (idea by coach) last_verified front matter, rendered on-page and ranked in CI — Add `last_verified: YYYY-MM-DD` to every page's front matter, print it in the page footer via a layout override, and have CI emit a staleness table sorted worst-first. The eight GitBook-era pages and privacy-policy.md (Last Updated: January 22, 2025) surface immediately. [trust/M/stretch]
- (idea by coach) _data/screenshots.yml — images expire like certificates — Register each of the 62 PNGs with a capture date and the app screen it depicts. CI warns at 180 days and fails at 365. find-phone-numbers-1.png (2026-06-07, shows the real "0 credits" header) passes; the eight 2024-10 GitBook screenshots that show a UI the app no longer has fail on the first run. [content-ops/M/stretch]
- (idea by coach) Support page owned by the inbox, with a 48-hour answer-to-doc rule — One /support page: a single address (retire the mykola@ / support@ split across faq.md, filter-leads-with-ai.md, privacy-policy.md and tos.md), stated hours, response target, status link, and a visible "Recently answered" list. Rule: any ticket answered in email must land as a doc edit within 48h, and the page shows the date of the last such edit. [support-deflection/M/stretch]
- (idea by coach) Sell the enrichment: a SignalsAPI credit bundle beside the affiliate links — Add a managed-enrichment SKU page, then attach a one-line disclosure include plus rel="sponsored nofollow" to all 22 ?via=signalsapi / ?fp_ref=signalsapi links across compare-people-data-providers.md, bring-your-own-people-provider.md and how-it-works.md:26. [conversion/M/stretch]
- (idea by coach) Explode the changelog into 105 dated, linkable, feature-cross-referenced pages — Render each _data/releases.yml entry as its own URL (/whats-new/2026-04-14-recruit-crm), and auto-inject a "Changes to this feature" block on every feature page from entries tagged with it — so integrations.md finally surfaces the Recruit CRM entry that today exists only at whats-new/index.md:25. [seo/L/stretch]
- (idea by coach) Publish llms.txt, an OpenAPI spec, and the docs themselves as an MCP server — Ship /llms.txt and /llms-full.txt at the site root, generate an OpenAPI 3.1 spec from features/api-access.md and features/agent-data-plane-api.md, and stand up an MCP server whose tools search and return these docs — so Claude, Cursor and Clay agents can read SignalsAPI documentation without scraping HTML. [developer-experience/L/wild]
- (idea by coach) Keyless live demo call embedded in the agent-data-plane pages — Expose one rate-limited public endpoint — hiring motion for a company domain — and embed a working "try it" box on agent-data-plane.md and agent-data-plane-clay.md, prefilled with a real domain, returning real `opened` / `reposted` / `reobserved` events from the ledger. [developer-experience/L/wild]
- (idea by coach) Publish the hiring-events ledger's public stats page — An auto-refreshing /panel page: companies tracked, events written per day, repost rate by industry and country, median days from requisition to signal — computed from the append-only ledger, no login, no key, updated on a schedule. [seo/L/wild]
- (idea by coach) Every app empty state deep-links into the exact doc anchor — Wire app.signalsapi.com empty and degraded states to docs anchors: empty leads list → the four-branch diagnostic; blank phone column → find-phone-numbers#leadmagic; "Qualified" badge with no decision-makers → bring-your-own-people-provider; 403 from the API → api-access#authentication. [onboarding/L/wild]
- (idea by coach) Publish the docs-health scorecard at /docs-health, burn-down and all — A public page listing the 81 audited findings by severity with status and fix dates, alongside the live CI assertions (link check, glossary lint, changelog freshness, screenshot age, per-page description coverage). Updated automatically by the same CI run that enforces them. [trust/M/wild]
- (technique by coach) started SCAMPER Method
- (idea by coach) Ship "No leads? Start here" — the seven-branch empty-results runbook — One new page, /troubleshooting/no-leads, structured as a decision tree over the seven real causes that produce an empty leads list: no provider key saved (free tier, bring-your-own-people-provider.md:14), "Email is required" on with a provider that returned no verified email, DNC domain match (do-not-contact-list.md), stop words firing in advanced-search.md:44, "Companies can be matched again" suppressing repeats (remove-duplicate-signals-2.png), credits exhausted/expired (whats-new/index.md:53), and Qualified-but-no-decision-makers (the gray badge from whats-new/index.md:15-21). Each branch names the exact screen and the exact field. [support-deflection/M/safe]
- (idea by coach) Anatomy of a lead: index the docs backwards from a CSV row — New page that opens with one literal exported row using the real header from api-access.md:172 (website,decision_maker_linkedin_url,decision_maker_email,decision_maker_phone,signal_job_title) plus the 20 Clay webhook fields from integrations.md:56-77. Every column becomes a link to the setting that produced it: signal_job_title → advanced-search, decision_maker_email → bring-your-own-people-provider, decision_maker_phone → find-phone-numbers, ai_field_1-5 → personalize-emails-with-ai. Reverses the reading order from config-first to output-first. [onboarding/M/stretch]
- (idea by coach) Publish a "What we don't do yet" page and link it from the FAQ — One page, /limits, stating plainly: Salesforce is not a native integration (contradicting faq.md:73), the MCP server is code-complete but unhosted (agent-data-plane-mcp.md:20), the agent data plane has no public base URL (agent-data-plane-api.md:15), export rate limiting is not enforced (api-access.md:191), phones come only from LeadMagic (find-phone-numbers.md:12), and there is no DPA yet. Replace the beta-flavoured faq.md:81 "active development stage" line with a link to it. [trust/S/wild]
- (idea by coach) Delete the eight 2024 screenshots instead of re-shooting them — Remove the alt="" PNGs whose payload contradicts the current app: remove-duplicate-signals-1/2.png, find-decision-makers-1/2.png, do-not-contact-list.png, filter-leads-with-ai.png, personalize-emails-with-ai-1/2/3.png. Write out in prose what each showed — notably "Companies can be matched again → Within this list: After 1 month | Among all lists: Yes", which today exists only as pixels inside a 375px-wide image. [support-deflection/S/safe]
- (idea by coach) Promote API access and the agent data plane out of Features to top-level — Move features/api-access.md and the four agent-data-plane*.md pages to a top-level API section at nav_order 3, above Features. Set has_children on agent-data-plane.md and drop the "Agent data plane — " title prefixes. Add aux_links entries for App and API in _config.yml (currently commented out at lines 9-10). [developer-experience/S/safe]
- (idea by coach) Replace screenshots with copy-pasteable settings blocks — Adopt one convention — a fenced block listing literal field names and values as they appear in the app — and retrofit it first on remove-duplicate-signals.md and find-decision-makers.md, whose entire instructional payload currently lives in empty-alt PNGs. Example block: `Duplicates → Companies can be matched again → Within this list: After 1 month | Among all lists: Yes`. [support-deflection/M/safe]
- (idea by coach) Turn index.md into a six-tile task router, not a pitch — Replace the 325 words of pitch on index.md (ending at line 28 with the fillout.com/meet booking link) with a grid keyed to intent: "Set up my first search", "Connect a people-data provider", "Get my leads into my CRM", "Call the API", "Understand credits", "Something's broken". Keep three sentences of what the product is; move the pitch and the Virtual Networker cross-sell to signalsapi.com where they belong. [conversion/S/safe]
- (idea by coach) Add a 404.md carrying a GitBook-era URL redirect map — Create 404.md with permalink: /404.html, the theme layout (so search comes free), and a client-side map of old GitBook paths to current ones — the export artifacts (filter-leads-with-ai.md:32 `[ai-variables.md](ai-variables.md "mention")`, the backslash-escaped variable names in ai-variables.md) prove the site migrated and old links are live in the wild. [seo/S/safe]
- (idea by coach) Merge the two Clay pages into one with a direction chooser — Fold features/integrations.md:40-79 (push: SignalsAPI webhooks 20 flat lead fields into a Clay table) and features/agent-data-plane-clay.md (pull: Clay HTTP column calls /v1/clay/enrich with a domain, gets 12 hiring-motion columns) into a single Clay page opening with a two-row table — "I want my leads in Clay" vs "I want Clay to score companies I already have" — then two setup sections beneath. [support-deflection/M/stretch]
- (idea by coach) Publish /llms.txt and .md twins — docs an agent can actually read — Add /llms.txt (indexed summary + link list) and /llms-full.txt (concatenated content) at the site root, plus raw-markdown twins at each page URL. Prioritise the agent-data-plane pages, api-access.md, advanced-search.md and compare-people-data-providers.md. [seo/S/stretch]
- (idea by coach) Ship a public sandbox key and a seeded company set, kill the email gate — Stand up a read-only sandbox base URL with ~50 frozen companies (zollsoft.de already appears as the worked example in agent-data-plane-clay.md and api-access.md) and print the demo key directly in agent-data-plane-api.md, replacing the three "email mykola@ to get set up" blocks at api-access:15, mcp:24 and clay:42. Every curl example on the page then runs as pasted. [developer-experience/L/wild]
- (idea by coach) Benchmark all eight people-data providers and publish the numbers — Run a fixed 500-company sample through Anymail Finder, Icypeas, People Data Labs, Prospeo, Snov.io, Hunter, LeadMagic and Tomba; publish match rate, verified-email rate, mobile-hit rate (LeadMagic), and cost per usable lead as a new section of compare-people-data-providers.md, with the methodology stated and a rerun date. Add rel="sponsored" to the 22 affiliate links while you are in the file. [seo/L/wild]
- (idea by coach) Run a docs MCP server so agents can query the docs they build against — Ship `signalsapi-docs` as a second MCP server alongside the planned `signalsapi-plane`, backed by the lunr index already built at _site/assets/js/search-data.json (155 KB). Tools: search_docs, get_page, list_endpoints. Document it on agent-data-plane-mcp.md next to the eight plane tools. [developer-experience/M/wild]
- (idea by coach) Stop reselling your customers' budget to eight affiliates — Remove the 22 ?via=signalsapi / ?fp_ref=signalsapi links from compare-people-data-providers.md, bring-your-own-people-provider.md, integrations.md and how-it-works.md:26, and replace the bring-your-own-only model with a bundled enrichment SKU priced in SignalsAPI credits. compare-people-data-providers.md becomes an honest "use your own key or use ours" chooser rather than neutral advice carrying tracking parameters. [conversion/L/wild]
- (idea by coach) Tag changelog entries by page, render "Recent changes" on the feature page — Add a `page:` slug to each entry in whats-new/index.md and render the matching entries as a dated block at the foot of the feature page. The 18.06.2026 job-description-required-words entry lands on advanced-search.md, the 16.04.2026 status-badge entry on a signals page, the 14.04.2026 Recruit CRM entry on integrations.md — which does not mention Recruit CRM at all today. [content-ops/M/stretch]
- (idea by coach) Publish the 290 sources as a real page, by country and board — New /sources page listing every monitored board and source, grouped by country, seeded from what the changelog already names (TechInAsia, Reed, Justjoin, Jooble, Finn.no, Jobindex, Monster, RocketJobs, HiringCafe, Indeed, LinkedIn) plus the ATS vendors the agent data plane observes (greenhouse et al., per agent-data-plane-clay.md `boards`). State refresh cadence per source. [seo/M/stretch]
- (idea by coach) Search recipe library: twelve named, copy-pasteable search configs — Convert advanced-search.md's abstract field reference into a sibling page of complete configs — "UK construction project managers, remote-friendly, excluding agencies" giving literal values for Search for, Semantic search, Job title required words, Job description required words, Stop words, Skills, Location patterns, Max age, Exclude staffing agencies, Hard to fill. Twelve recipes across the verticals the product sells into. [onboarding/M/stretch]
- (idea by coach) Build a filter simulator that runs in the browser, no account needed — A static JS page (no external hosts, inline everything) with ~15 realistic sample job signals baked in, plus inputs for Job title required words, Job description required words, Stop words, Skills required/excluded and Work arrangement. Typing shows live which signals survive and which rule killed each one — the OR-within/AND-across logic from advanced-search.md:46 made visible. [conversion/L/stretch]
- (idea by coach) Credits page with a provider-spend calculator, not just a definition — New /credits page defining what consumes a credit, per-plan allowance, the 60-day expiry (today only at whats-new/index.md:53), free-tier limit of 150, and run frequency by plan — plus a calculator estimating monthly provider spend from companies/month × people per company × chosen provider, using the at-source vs after-fetch table in compare-people-data-providers.md:51-60 to model the waste each provider causes. [conversion/M/stretch]
- (idea by coach) Dissolve "Features" — re-parent all 20 pages into task sections — Delete features/index.md and the parent: Features front-matter noun entirely. Re-parent into Set up (bring-your-own-people-provider, compare-people-data-providers, credits), Find signals (advanced-search, location-based-filtering, work-arrangement-filter, website-monitoring, remove-duplicate-signals), Reach people (find-decision-makers, find-phone-numbers, do-not-contact-list), Personalize (filter-leads-with-ai, personalize-emails-with-ai, ai-variables), Deliver (integrations), API. Move request-new-feature.md to the footer. [onboarding/L/stretch]
- (idea by coach) Shard the 729-line changelog by year and strip its 53 screenshots — Split whats-new/index.md into /whats-new/2026, /2025, /2024 with the current year as the section index; delete the 53 PNGs (1.9 MB, all alt="", several 3359px wide rendered at ~800px) or move them behind per-entry links. Give every entry a stable anchor. [seo/S/safe]
- (idea by coach) Ship a help-anchor contract file the app reads for its "?" icons — Add _data/help_anchors.yml mapping app screens to doc URLs — settings.provider → bring-your-own-people-provider, persona.phone → find-phone-numbers#enable-phone-lookup, filters.work_arrangement → work-arrangement-filter, leads.dnc → do-not-contact-list, project.empty → the no-leads runbook — and publish it as JSON. The app renders contextual help links from it; CI fails if an anchor in the file no longer exists in the built site. [support-deflection/M/stretch]
- (idea by coach) Generate api-access and /v1 reference from one OpenAPI 3.1 spec — Author openapi.json covering both surfaces — the self-serve read API (GET /projects/, /projects/{id}/leads, /projects/{id}/leads/export) and the plane's /v1 routes — serve it at docs.signalsapi.com/openapi.json, and render api-access.md and agent-data-plane-api.md from it. Ship a Postman collection and an httpie snippet set alongside. [developer-experience/L/stretch]
- (idea by coach) A five-day onboarding course whose entire body is docs links — Five emails sent from signup: Day 1 create your first search (the missing walkthrough — Search → Add Search → keywords → location → save), Day 2 connect a provider key, Day 3 build a persona and find decision-makers, Day 4 write one AI prompt using ai-variables, Day 5 push to your CRM or pull via the API. Each email is three sentences plus one docs link; the docs pages are the curriculum. [onboarding/M/stretch]
- (idea by coach) Every page declares the ticket it deflects, reviewed monthly — Add a `deflects:` front-matter key listing the real support questions each page answers, seeded from the inbox at mykola@signalsapi.com. Pages with an empty `deflects:` (request-new-feature.md, is-it-working.md, features/index.md, location-based-filtering.md) are candidates for deletion. Run a monthly pass: any question answered twice by email becomes a page or an FAQ entry. [content-ops/M/stretch]
- (idea by coach) Strip prices from faq.md and stop selling a phone add-on that no longer exists — Delete faq.md:51 (£49/£149/£199 phone packages) and the flat £400/€490/$550 at faq.md:26; replace with a pointer to a real pricing page and to find-phone-numbers.md, which states phones come from the customer's own LeadMagic key at no SignalsAPI credit cost. Also fix "propsects", "100% reliable." at line 65, and the Salesforce claim at line 73. [trust/S/safe]
- (technique by coach) started Worst Possible Idea
- (idea by coach) Publish a real leads.csv and API response anyone can download — Add a /samples/ section with three no-login artifacts: samples/leads-sample.csv containing the exact five columns api-access.md:172 declares (website, decision_maker_linkedin_url, decision_maker_email, decision_maker_phone, signal_job_title), samples/leads.json with the full nested person/company shape from the GET /projects/{id}/leads example, and samples/hiring-pulse.json showing a real provenance envelope with as_of, X-Data-Freshness and X-Meter-Class. Anonymized real rows, not lorem. [conversion/M/stretch]
- (idea by coach) Enrichment spend calculator: subscription plus your provider's bill — A static-JS page at /pricing/calculator that takes leads/month, chosen provider from the eight in compare-people-data-providers.md, whether phone lookup is on, and whether that provider filters title/country/city at source or after fetch — then outputs the SignalsAPI subscription plus the estimated provider bill, with after-fetch waste as its own line item. [conversion/M/stretch]
- (idea by coach) Demote screenshots, promote field tables on the GitBook-era pages — Rewrite remove-duplicate-signals.md, do-not-contact-list.md, find-decision-makers.md and website-monitoring.md so a settings table is the payload and the PNG is garnish. remove-duplicate-signals gets a real table: control 'Companies can be matched again', options 'Within this list' (Never / After 1 month / ...) and 'Among all lists' (Yes / No), with defaults and the effect each has on lead volume — values currently legible only by squinting at remove-duplicate-signals-2.png. [support-deflection/M/safe]
- (idea by coach) One footer include that deflects the top five tickets sitewide — Create _includes/footer_custom.html with a 'Before you email us' block linking the five known highest-volume confusions: no leads at all (free tier, no provider key), no phone numbers (LeadMagic only), what a credit is and the 60-day expiry, what 'Qualified' vs 'Leads found' vs 'Searching...' mean, and run frequency by plan. Then strip the seven scattered 'Contact me at mykola@signalsapi.com' CTAs and normalise on one address. [support-deflection/S/safe]
- (idea by coach) Own the ledger vocabulary with a hiring-signals glossary — A top-level /glossary defining the terms only SignalsAPI uses: opened / reposted / reobserved events, hiring pulse, repost pain, first hire, ATS migration, role demand, change feed, hard-to-fill, hiring surge, signal, persona, lead list. Each entry gets a plain definition, the app control that uses it (advanced-search.md's 'Hard to fill' and 'Hiring surge'), and the API primitive that returns it (agent-data-plane-api.md endpoints). [seo/M/stretch]
- (idea by coach) Make the docs agent-readable: llms.txt, OpenAPI, MCP manifest — Ship /llms.txt and /llms-full.txt (curated markdown index plus full corpus), an /openapi.json describing api.signalsapi.com's three read endpoints, and a /v1 plane spec covering the eight primitives agent-data-plane-mcp.md tabulates. Reference all three from api-access.md and agent-data-plane.md. [developer-experience/M/wild]
- (idea by coach) Publish a docs freshness dashboard that names stale pages — Add `updated:` and `verified_against:` to every page's front matter, render 'Last verified 2026-07-31 against the Projects UI' in the page header, and generate a public /docs-health page listing all 28 pages sorted by age — which today would openly show the eight GitBook screenshots at 21 months and privacy-policy.md at 18. [trust/M/wild]
- (idea by coach) Anatomy of a signal: trace one real company end to end — Replace how-it-works.md's twelve agentless passive bullets with one traced example: a real posting observed at T0, the `opened` ledger event it writes, the ICP filter that kept it, the persona that ran, the provider call that returned the person, the verified email, the CSV row, the CRM push. Each object is named where it first appears — search, filter, persona, lead list, project, credit — with the app screen it lives on. [onboarding/M/stretch]
- (idea by coach) Clay router page: push leads out or enrich companies in — Add features/clay/index.md as a parent that asks one question — are you pushing SignalsAPI leads into a Clay table, or calling SignalsAPI as an enrichment column on companies you already have? — then routes to the two children, retitled 'Clay — push leads to your table' (from integrations.md:40) and 'Clay — enrich companies in a column' (agent-data-plane-clay.md), nested beneath it. [conversion/S/safe]
- (idea by coach) Turn the provider comparison into a measured, disclosed, monetized ranking — Rebuild compare-people-data-providers.md with measured numbers from real runs — median match rate, verified-email rate, fetched-vs-disqualified ratio per provider — add a one-line affiliate disclosure at the top, put rel="sponsored nofollow" on all 22 ?via= / ?fp_ref= links, and add a ninth row: SignalsAPI Managed enrichment, so the page has a house option. [trust/L/wild]
- (idea by coach) Generate the changelog from feature pages, not the other way round — Move changelog entries into each feature page's front matter as a `changelog:` list, build whats-new from them, split the 729-line page by year (whats-new/2026.md, 2025.md, 2024.md) with per-entry anchors, and emit a feed.xml. Add a 'Shipped 18.06.2026' badge to each feature page from the same data. [content-ops/M/stretch]
- (idea by coach) Empty-results triage: why you have no leads, emails, or phones — One page, three decision trees. No leads at all → no provider key means free tier, company signals only; or filters too tight; or the 'Qualified' badge means matched but no decision-maker found. No emails → confidence below 70% is withheld. No phones → LeadMagic is the only provider returning mobile, and Find phone numbers must be enabled. Every branch ends in a link to the page that owns the detail. [support-deflection/S/safe]
- (idea by coach) Cost one real search run, credit by credit — A Credits page that defines the unit, then walks one actual run line by line: what the search cost, what qualification cost, what enrichment cost, what the free tier's 150 credits buy. Include the negative list explicitly — people lookups, API reads and the export endpoint spend nothing (api-access.md:180, 188) — plus the 60-day expiry currently stated only at whats-new/index.md:53. [support-deflection/S/safe]
- (idea by coach) Spin the agent data plane out with a qualifying waitlist — Promote the four agent-data-plane pages out of Features into their own top-level section, and replace 'email mykola@signalsapi.com' with a waitlist form capturing usage shape: expected calls/month, which primitives (hiring pulse, first hire, repost pain, ATS migrations, role demand, change feed), REST or MCP, and target go-live. Publish indicative Tier 0 / Tier 1 pricing bands and stated rate limits alongside it. [conversion/M/stretch]
- (idea by coach) Ship /plane-status.json so integrators poll instead of emailing — Publish a machine-readable manifest listing every plane primitive and MCP tool with a state (live / code-complete / planned), its base URL once one exists, and a last-changed timestamp — mirrored as a status column in the agent-data-plane-mcp.md tool table and the agent-data-plane-api.md endpoint list. [developer-experience/S/wild]
- (idea by coach) One CI gate for links, front matter, alt text, and publish surface — Extend .github/workflows/ci.yml beyond its single `jekyll build` step: html-proofer (catches filter-leads-with-ai.md:32's 21-month-old dead link to ai-variables.md), a front-matter schema check (unique nav_order — privacy-policy.md and is-it-working.md both claim 6 — plus a non-empty per-page description), an alt-text check rejecting `![](` and alt="", an image weight cap, a WCAG contrast check on the *:focus rule, and a rule failing the build if _site's root contains any file without front matter. [content-ops/S/safe]
- (idea by coach) Redirect map rescuing 21 months of GitBook-era inbound links — Add jekyll-redirect-from plus a redirects file covering the old GitBook paths, the `.md` link variants the export left behind, and the doubled-slash `//features/...` canonical form Google has been indexing — all landing on their current pages. Pair it with a branded 404.md carrying the sidebar and lunr search box, and set permalink: pretty so authored links and emitted URLs finally agree. [seo/S/safe]
- (idea by coach) One billing truth table, included in five places at once — Author a single include stating what SignalsAPI bills (subscription, credits, 60-day expiry), what your provider bills (people search, email lookup, phone lookup — LeadMagic only), and what is free (API reads, CSV export, company signals on the free tier). Embed it verbatim in faq.md, tos.md's fee section, bring-your-own-people-provider.md, find-phone-numbers.md and api-access.md. [trust/S/safe]
- (idea by coach) Render integrations.md from _data/integrations.yml — Move every integration into a data file with typed fields — system, direction (push/pull), object created, dedup rule, plan requirement (Recruit CRM needs Business tier per whats-new/index.md:25), setup-doc status — and render both the list and the sections from it. Adding Recruit CRM, Instantly and Webhook becomes a data edit; Salesforce becomes an explicit 'via webhook only' row instead of an unbacked FAQ promise. [content-ops/M/safe]
- (idea by coach) Search recipes: copy-pasteable configurations per vertical — A /recipes section with eight to ten complete, named search configurations, each giving exact values for advanced-search.md's fields — semantic terms, job-title required words, description required words, stop words, skills, job family, location patterns, max posting age, exclude-staffing-agencies, hard-to-fill or hiring-surge — plus expected volume and the first thing to loosen when results are thin. [onboarding/M/stretch]
- (technique by coach) started Trait Transfer
- (idea by coach) Pharmacy-leaflet "Do not start unless" panel on the four gated pages — Ship a reusable `_includes/prereq.html` styled as a PIL contraindication box and render it above the fold on features/find-decision-makers.md, features/find-phone-numbers.md, features/filter-leads-with-ai.md and features/personalize-emails-with-ai.md. Copy is blunt and negative: "No provider key saved → this page returns nothing. Provider is not LeadMagic → the Find phone numbers checkbox does nothing. Free tier → {phone}, {headline} and every person variable render empty." Each ends with one link to bring-your-own-people-provider. [support-deflection/S/safe]
- (idea by coach) Michelin stars and a published rubric on the provider comparison — Rewrite features/compare-people-data-providers.md's headline table to award each of the eight providers a symbol with a legend — ★ best default, ▲ worth a detour for one specific thing, ● only if you already have an account — scored against a published rubric (at-source filter count, mobile, headline, one secret vs two). Add the Michelin-style legend block and, inside it, the one-line affiliate disclosure covering the 14 `?via=signalsapi` / `?fp_ref=signalsapi` links already on the page. [trust/S/safe]
- (idea by coach) Give the chrome a seat pocket: aux_links plus a real footer — Uncomment and replace the stub at _config.yml:9-10 with aux_links for Open app / Start free / Pricing / Status, and add `_includes/footer_custom.html` carrying support address, signalsapi.com, Privacy, Terms and Request a feature. Set `nav_exclude: true` on privacy-policy.md, tos.md and features/request-new-feature.md so they move out of product nav into the footer. [conversion/S/safe]
- (idea by coach) Jump-to-recipe router above the pitch on index.md — Insert three cards immediately under the H1 of index.md — "I'm setting up" → getting started, "Something isn't working" → troubleshooting, "I'm building on the API" → api-access / agent-data-plane — and demote the existing pitch (lines 11-26) below them. Delete the closing sales-call line at index.md:28 in favour of a Start free link plus the consultation link as a secondary option. [onboarding/S/safe]
- (idea by coach) Retitle the eight support-load pages as the questions people type — Change front-matter titles and H1s so the page's name is the query: remove-duplicate-signals.md → "Why am I seeing the same job twice?", is-it-working.md → "Is SignalsAPI down?", work-arrangement-filter.md → "How do I find only remote roles?", find-phone-numbers.md keeps its title but gains an H2 "Why is my Phone column all dashes?". Pair each with a one-sentence `description:` that repeats the question, and make the first paragraph the direct answer. [seo/S/safe]
- (idea by coach) Symptom-first troubleshooting section, written like a recipe FAQ — Create /troubleshooting/ with one short page per observed symptom: "My leads list is empty", "The Phone column is all dashes", "Every signal shows the grey Qualified badge", "Leads stopped appearing this week", "My AI prompt output is blank", "My integration destination received nothing". Each is symptom → the two or three causes (free tier, invalid key after provider rotation, LeadMagic not selected, 60-day credit expiry, Manual Approval left on) → the exact fix. Link every one from faq.md. [support-deflection/M/safe]
- (idea by coach) Stripe-grade error dictionary for both APIs — Add an "Errors" section to features/api-access.md and features/agent-data-plane-api.md listing every status the API emits with its literal JSON body and the one thing to change: 403 missing/invalid key, 403 deactivated key, 404 not-your-project, 422 unsupported `format`, and on the plane the 202 `{"job_id": …, "status": "crawling"}` with the poll pattern spelled out. Give each error an anchor so support can paste a deep link. [developer-experience/M/stretch]
- (idea by coach) Ship llms.txt, copy-as-markdown, and an MCP server for the docs themselves — Generate /llms.txt (page index with one-line summaries) and /llms-full.txt (whole corpus) at build time, add a "Copy page as Markdown" button via a small `_includes` snippet, and stand up docs.signalsapi.com/mcp exposing search_docs and get_page. Announce it on features/agent-data-plane-mcp.md as the reference implementation. [developer-experience/M/stretch]
- (idea by coach) Tax-form pricing: three worked baskets with the arithmetic shown — Create /pricing.md — the page tos.md §5 already cites and that 404s — built around three named scenarios with the sums written out, tax-help-text style: solo recruiter (1 search, ~200 signals/month, People Data Labs) → SignalsAPI £400 + ~$28 provider; agency (4 searches, LeadMagic + phones) → £400 + ~$X; agent builder (Tier 0 cached reads only) → £0. Include the plan table, the free-tier boundary, the 60-day credit expiry, and delete the dead £49/£149/£199 phone add-on from faq.md:51. [conversion/M/stretch]
- (idea by coach) IKEA parts inventory: everything you need before step 1 — Write getting-started.md at top-level nav_order 2 opening with a bill of materials rendered as an inventory panel — an account, your ICP in one sentence, optionally one people-data provider key (with "skip this and you stay on the free tier: company signals only"), optionally a destination integration — followed by numbered steps each carrying a time estimate ("Create your first search — 4 min", "Wait for the first crawl — up to 30 min, walk away"). [onboarding/M/stretch]
- (idea by coach) Tutorial-level gating: each step ends in an observable proof — Structure that getting-started walkthrough as four gated rooms, each closing with a checkable outcome and an escape hatch: "You should now see signals with a blue Searching… badge" / "…a green Leads found badge" / "…a Phone column" / "…rows in your Clay table". Under each proof, a single line: "Not seeing it? →" linking the matching /troubleshooting symptom page. [onboarding/M/stretch]
- (idea by coach) The airline safety card: one printable page of the whole object model — Build /cheatsheet — a single screen, near-wordless, showing the pipeline Search → Filter → Persona → Lead list → Destination as one diagram with the app screen each object lives on, the three signal badges and what they mean, the three reasons people data comes back empty, and the credit rules. Give it a print stylesheet and a PNG export, and state the project ≡ lead list equivalence once, right there. [onboarding/M/stretch]
- (idea by coach) One fictional agency, every page: Acme Recruitment hiring QA in Manchester — Pick a single worked example and run it through advanced-search.md, find-decision-makers.md, ai-variables.md, personalize-emails-with-ai.md, integrations.md and api-access.md: the same search terms, the same persona, the same three leads, the same CSV rows, the same Clay columns, the same JSON `project_id`. Replace api-access.md's project 654 "Customer Success"/zollsoft GmbH, find-phone-numbers.md's acme.com/widget.co and advanced-search.md's construction PMs with it. [content-ops/L/stretch]
- (idea by coach) Cloudflare-style /trust hub with a named sub-processor table — Add /trust listing the eight people-data providers as named sub-processors (matching the compare-people-data-providers.md table exactly), where and how customer provider credentials are stored — upgrading bring-your-own-people-provider.md:89-92's "Your key is safe" from a marketing sentence into a stated commitment — a downloadable DPA, retention periods for decision-maker data, and the status.signalsapi.com link. Re-date privacy-policy.md and link it here. [trust/M/stretch]
- (idea by coach) Publish the numbers: a Limits and quotas page with no blanks — Create /limits stating every ceiling in one table: leads page size 1–500, export endpoint rate limiting currently unenforced (say so, Vercel-style), 60-day credit expiry, run frequency per plan, the actual Tier 0 freshness-floor interval in hours, the four meter units (call, change, watch, forced_fresh), and the 202 crawl turnaround. Link it from api-access.md:184 and agent-data-plane.md's metering section. [trust/M/stretch]
- (idea by coach) Twilio-style runtime picker: n8n and Make, not just curl — Add tabbed snippets to features/api-access.md and features/agent-data-plane-clay.md covering curl, Python, n8n HTTP node, Make.com, and the Clay HTTP enrichment column config — the same export or enrichment call in each. Put the same picker on the integrations webhook section that features/integrations.md:22 lists but never documents. [developer-experience/M/stretch]
- (idea by coach) Turn ai-variables.md into a variable inspector with failure values — Rebuild features/ai-variables.md as a table with columns: variable, description, example value (from the Acme worked example), provider requirement (free tier / any provider / LeadMagic only / Icypeas-PDL-Prospeo only for {headline}), and — the transferred trait — what it renders when unavailable. Add the missing ai_field_1–ai_field_5 that personalize-emails-with-ai.md:39 and integrations.md:77 both depend on, and unescape the GitBook backslashes. [support-deflection/M/stretch]
- (idea by coach) Generate the provider matrix and object glossary from _data YAML — Move the eight-provider matrix into `_data/providers.yml` (fields: email, mobile, linkedin, headline, credentials, at-source filters, affiliate URL, disclosure flag) and the object model into `_data/objects.yml`, then render both with Liquid into compare-people-data-providers.md, bring-your-own-people-provider.md and the cheatsheet. Add an html-proofer step to .github/workflows/ci.yml. [content-ops/M/stretch]
- (idea by coach) Demolish the changelog; give every feature page a Changed strip — Convert whats-new/index.md's 105 entries into a Jekyll `_posts` collection, one file per date, each tagged with the feature pages it touches. Render a "Changed" strip on every feature page pulling its own tagged entries, and backfill the changelog-only features into real pages — Recruit CRM into integrations.md, the three signal badges into a status page, the dedup modes into remove-duplicate-signals.md, credit expiry into /limits. [content-ops/L/stretch]
- (idea by coach) IKEA callouts baked into the nine feature screenshots — Re-shoot the eight 2024-10 screenshots against the current Projects-scoped UI and overlay numbered circles on each, keyed one-to-one to numbered prose steps below — so find-decision-makers-1.png carries ① Person country ② Emails required ③ Find phone numbers, and the prose reads "① Set the country…". Write real alt text describing what each numbered element is. [onboarding/M/stretch]
- (idea by coach) Error-code short URLs the app links from its own empty states — Publish stable short paths — /e/no-people, /e/invalid-key, /e/no-phone-provider, /e/credits-expired, /e/awaiting-approval, /e/no-signals — each a 150-word page with cause and fix, and have the app render them in its empty states, invalid-key banner and zero-result screens. Ship the mapping as a documented contract so app and docs stay in sync. [support-deflection/M/stretch]
- (idea by coach) A public read-only demo key that makes every curl block runnable — Mint a shared, rate-limited, read-only demo key over one frozen anonymised project and paste it literally into every snippet in features/api-access.md, replacing `YOUR_API_KEY`. A stranger copies the curl line, gets real JSON with real shapes back, then sees a one-line "That was demo data — your key is in Settings → API Key" callout. [conversion/L/wild]
- (idea by coach) Publish "When SignalsAPI is the wrong tool" — A short top-level page naming the disqualifiers plainly: you sell to consumers; you want candidates rather than clients (Recruit CRM leads land as sales-side contacts, not candidates); you need a native Salesforce object — delete that promise from faq.md:73 and say webhook-only here; you need sub-hour freshness (Tier 0 deliberately floors it); you have no people-data provider budget and need contacts on day one. End each with the honest alternative. [trust/S/wild]
- (idea by coach) The Signal Index: monthly market pages generated from the ledger — Generate and publish a free public section — one page per role family × country × month — straight from the append-only hiring-events ledger: "UK QA hiring, July 2026 — 1,240 roles opened, 380 reposted, median 41 days open, the 20 companies that could not fill". Each page ends with the search that would have surfaced those signals, deep-linked into the app. [seo/L/wild]
- (idea by coach) Live ICP checker on the docs site, powered by role-demand — Embed an interactive widget — role family + country + company-size band — that calls the agent data plane's role-demand and repost-pain primitives live and returns the last 12 months of demand plus a count of companies currently showing repost pain, with a "see these signals" link into signup. Place it on index.md, features/advanced-search.md and every Signal Index page. [conversion/L/wild]
- (technique by coach) started Kill the Crown Jewel
- (idea by coach) Move the six datasets into _data/; pages become templates — Extract the corpus that survives deleting features/: _data/providers.yml (the 8-provider capability + at-source/after-fetch matrices from compare-people-data-providers.md:20-60), _data/variables.yml (the 17 names in ai-variables.md plus ai_field_1-5), _data/integrations.yml (Clay, HubSpot, Apollo, Snov, Instantly, Bullhorn, Zoho, Firefish, Recruit CRM, Webhook), _data/filters.yml (the ~24 fields in advanced-search.md), _data/pricing.yml, _data/glossary.yml. Rewrite the surviving pages as five Liquid templates that render from those files. [content-ops/L/wild]
- (idea by coach) Replace the Features tree with a symptom router — Ship /troubleshoot as the top nav entry, keyed on the six observed failures the audit names: no leads returned, no phone numbers, blank email column, credits at zero, signals look stale, integration pushed nothing. Each entry is symptom → the one cause → the exact app deep link (app.signalsapi.com/personas, /filters/, Settings → Integrations). No feature page required. [support-deflection/M/stretch]
- (idea by coach) Publish /llms.txt and /all.md — the whole site in one paste — Generate an /llms.txt index of every page with one-line descriptions, plus /all.md: the entire docs corpus concatenated into a single markdown file, both built by a Jekyll page that loops site.pages. Link both from the footer. [developer-experience/S/safe]
- (idea by coach) Delete both REST pages; generate them from one openapi.yaml — Write openapi.yaml covering api-access.md's three routes (GET /projects/, /projects/{id}/leads, /leads/export) and the /v1 routes in agent-data-plane-api.md, then render it with a single self-contained viewer page. Delete the 3,700 words of hand-maintained endpoint prose. Generate the MCP tool table in agent-data-plane-mcp.md from the same file. [developer-experience/M/stretch]
- (idea by coach) Encode the prerequisite DAG; inject Requires banners automatically — _data/prereqs.yml states the chain: mobile phone → LeadMagic key → paid LeadMagic account; any person data → any connected provider key; no key → company signals only; Recruit CRM push → Business-tier Recruit CRM plan. A Liquid include renders a Requires banner at the top of every page from that data, and the same file renders as a Mermaid graph on how-it-works. [onboarding/M/stretch]
- (idea by coach) A credits-and-provider-spend calculator instead of a pricing page — Build /pricing as a calculator: inputs are companies per month, chosen provider, phone lookup on/off, and how many of your filters run at-source vs after-fetch. Output is SignalsAPI credits consumed, 60-day expiry warning, and estimated provider bill. The at-source/after-fetch data comes straight from _data/providers.yml. [conversion/M/stretch]
- (idea by coach) Turn eight affiliate links into eight ranking pages — Generate from _data/providers.yml: eight provider pages (/providers/leadmagic, /providers/prospeo, …) and the pairwise comparisons that matter (LeadMagic vs Prospeo, Hunter vs Tomba, People Data Labs vs Icypeas), each stating what SignalsAPI does with that provider's filters, plus a one-line affiliate disclosure. compare-people-data-providers.md becomes the hub. [seo/L/stretch]
- (idea by coach) Kill whats-new/index.md; ship releases.yml plus per-page change stamps — Delete the 729-line page and its 53 PNGs (1.9 MB). Replace with _data/releases.yml where every entry carries a required `page:` key naming the doc it belongs to. Render /whats-new as the latest 20 entries, emit feed.xml, and inject a 'Last changed 18.06.2026' stamp onto each feature template. Entries whose `page:` is empty fail the build. [content-ops/M/stretch]
- (idea by coach) Delete all 17 screenshots; ship UI breadcrumbs instead — Remove remove-duplicate-signals-1/2.png, find-decision-makers-1/2.png, personalize-emails-with-ai-1/2/3.png, work-arrangement-filter-1/2.png, do-not-contact-list.png, filter-leads-with-ai.png, byo-people-provider-1-4.png and find-phone-numbers-1/2.png. Replace each with the literal click path in text — `Search → Add Search → Website Monitoring`, `Settings → Integrations → Recruit CRM` — plus the app deep link. [content-ops/S/safe]
- (idea by coach) Collapse 28 pages into one scrolling URL with anchors — Concatenate the site into a single page with a sticky in-page table of contents and stable anchors (#advanced-search, #providers, #v1-hiring-pulse). The just-the-docs sidebar becomes a ToC of anchors on one document; the 28 separate URLs 301 to their anchors. [seo/L/wild]
- (idea by coach) Home page proves the claim: live signal counter, running query — Replace index.md's 325 words of pitch and its closing 'book a free consultation' with three things: a live count of signals detected in the last hour across the 290 sources, one working search box that queries the public API and shows real hits, and three links (Start free, Troubleshoot, /v1 reference). [conversion/M/wild]
- (idea by coach) Ship a public sandbox key so the data plane stops emailing a founder — Stand up a read-only demo base URL and a published sandbox key scoped to a fixed sample account, and make every curl block in agent-data-plane-api.md, -mcp.md and -clay.md executable as printed. Keep production access gated, but replace 'no public base URL, email mykola@' with 'try it now, then request production keys'. [conversion/M/wild]
- (idea by coach) faq.md becomes _data/faq.yml with FAQPage JSON-LD — Convert the 16 Q&As to data and render them as an accordion plus schema.org FAQPage markup. Each entry gets a topic tag so the credits answer also renders on /pricing and the freshness answer on the data page. is-it-working.md is deleted into the status entry. [seo/S/safe]
- (idea by coach) CI asserts claims, not just links — Extend .github/workflows/ci.yml with html-proofer over the 42 relative links plus custom assertions: every price string must exist in _data/pricing.yml, every vendor name must exist in _data/providers.yml or _data/integrations.yml, no img may have empty alt, every page must declare a front-matter description, no page may contain 'personation'. Fail the build on violation. [content-ops/S/safe]
- (idea by coach) Retire the noun Features; navigate by verb — Delete features/index.md and the Features parent entirely. Top-level sections become Find signals, Qualify companies, Get contacts, Deliver leads, Build on the API — with the four agent-data-plane pages under the last. Every page's parent front matter is rewritten accordingly. [onboarding/M/stretch]
- (idea by coach) Make docs URLs part of the app's error surface — Wire the product's empty and blocked states to specific docs anchors: the zero-leads state links /troubleshoot#no-leads, the '0 credits' header links /pricing#credits, a disabled phone checkbox on the Persona form links /providers#leadmagic, and the 'Qualified' (no decision-makers found) badge links /troubleshoot#no-people. Ship the anchors first, then the app links. [support-deflection/M/wild]
- (idea by coach) Turn advanced-search.md into a search-config builder — Convert the site's best asset — 24 documented fields across keyword/semantic search, title and description required words, stop words, skills, job families, location patterns, posting age, staffing-agency exclusion, hard-to-fill and hiring-surge — into an interactive form that emits the exact search configuration plus a deep link into the app, with inline notes on what each field removes. [seo/L/stretch]
- (idea by coach) Publish measured freshness instead of claiming real-time — Build /data-freshness that reads the plane's own X-Data-Freshness header and publishes median and p90 observation lag by source class (ATS, job board, LinkedIn, funding, filings), refreshed daily, with the Tier 0 freshness floor stated explicitly. Replace faq.md:69 'Signals are updated in real-time.' with a link to the live number. [trust/M/wild]
- (idea by coach) One support page; retire is-it-working.md and the personal inbox — Create /support with a single address, stated hours and response target, the status.signalsapi.com link, and a triage form that pre-fills project id and search id. Delete is-it-working.md into it, and normalise every mailto — the six mykola@ instances across faq.md, filter-leads-with-ai.md and the three agent-data-plane pages, and the support@ instances in the legal pages — to one address. [support-deflection/S/safe]
- (idea by coach) Three questions pick the provider and show the bill — Replace the 'How to choose' bullets in compare-people-data-providers.md with a widget: need mobile numbers? filter by country or city? need LinkedIn headlines for AI copy? It returns the provider, the filters that will run at source versus after fetch, the expected extra spend, and a disclosed affiliate link. [conversion/M/stretch]
- (idea by coach) _data/glossary.yml auto-links first mention of every product noun — Define signal, search, filter, persona, lead list, project, credit, forced_fresh, opened/reposted/reobserved once in _data/glossary.yml with the canonical UI label for each, and add a Liquid filter that auto-links and hover-defines the first occurrence on every page. Retire 'personation' entirely and pick one of project/lead list as canonical. [onboarding/M/stretch]
- (idea by coach) Serve the docs themselves over MCP, before the data plane ships — Deploy a small MCP server exposing the docs corpus as tools — search_docs, get_page, list_prereqs — pointed at the same _data files the site renders from, and document it on agent-data-plane-mcp.md as the first live endpoint. [developer-experience/M/wild]
- (technique by coach) started Relativity Frame Shift
- (idea by coach) Symptom-titled troubleshooting pages the app can deep-link to — Add /troubleshooting/ with four pages named after failure states, not features: "My leads list is empty" (free-tier gate, from bring-your-own-people-provider.md:15), "The Phone column is blank" (LeadMagic-only, from find-phone-numbers.md:12), "My credits disappeared" (60-day expiry, from whats-new/index.md:53), "My search hasn't run this week" (run frequency by plan, from whats-new/index.md:612). Each gets a stable anchor per cause; hand the anchor list to the app team so empty states link straight in. [support-deflection/M/safe]
- (idea by coach) Ship a "What SignalsAPI does not do" page before a competitor writes it — One page listing the honest limits: no Salesforce integration (delete the claim at faq.md:73), no bundled people data — you connect your own provider key, mobile numbers only via LeadMagic, the agent data plane has no public base URL yet, and Tier 0 reads are deliberately floored on freshness so "real-time" (faq.md:69) means the floor, not the second. Link it from index.md and faq.md. [trust/S/safe]
- (idea by coach) Transcribe the three videos into page text — integrations.md:136 and :140 are bare YouTube URLs for Zoho and Firefish — the entire documentation for two integrations. Transcribe both into numbered setup steps in-page, and transcribe the 1-min demo (youtube.com/watch?v=jB4DIDnfnTQ, linked from index.md:23 and faq.md:13) into a short "what happens in the product" section on how-it-works.md. [seo/S/safe]
- (idea by coach) Every page gets an orientation strip and a task-shaped CTA — Add two _includes partials rendered by the layout: a one-line strip above the H1 ("SignalsAPI turns hiring signals into qualified leads — see how it works · start free") and a footer CTA block chosen by a `cta:` front-matter key. advanced-search.md and compare-people-data-providers.md — the two pages most likely to rank — currently carry zero product context and zero conversion action between them. [conversion/S/safe]
- (idea by coach) One Clay page that owns both directions of data flow — Merge integrations.md's Clay section (:40, SignalsAPI pushes 20 flat fields to a Clay webhook) and agent-data-plane-clay.md (Clay pulls an enrichment column given a domain) into a single Clay hub with a two-row chooser table: "I want new leads to land in my table" → push setup; "I already have domains and want their hiring motion" → enrichment column. Add a third row for the combined pattern — push leads in, then enrich the same rows with the plane column. [support-deflection/S/safe]
- (idea by coach) Answer capsules: one summary field, four consumers — Add a 40-word `summary:` to the front matter of the 10 commercial pages (api-access, agent-data-plane*, compare-people-data-providers, advanced-search, bring-your-own-people-provider, integrations, find-phone-numbers, faq). Render it three ways: a bordered "In short" block above the H1, the page's `<meta name="description">`, and an extra field injected into the lunr search-data.json. [seo/M/stretch]
- (idea by coach) Publish /llms.txt and /llms-full.txt — Commit an llms.txt at the site root: what SignalsAPI is in three lines, then a curated link list grouped by intent (Start here / Search & filter reference / People-data providers / REST API at api.signalsapi.com / Agent data plane preview / Pricing & credits / Trust). Generate llms-full.txt as the concatenated markdown of the 12 pages worth quoting, excluding the changelog and legal pages. [seo/S/stretch]
- (idea by coach) Commit OpenAPI 3.1 specs for both REST surfaces — Write /openapi.yaml covering the three live api.signalsapi.com routes documented in api-access.md (GET /projects/, GET /projects/{id}/leads, GET /projects/{id}/leads/export) and /agent-plane-openapi.yaml for the /v1 routes in agent-data-plane-api.md. Link both as downloads from their pages, and note the plane spec is the contract, pending a base URL. [developer-experience/M/stretch]
- (idea by coach) Move the 8-provider matrix into _data/providers.yml — One YAML record per provider with fields for email, mobile, linkedin, headline, credential shape, per-filter at-source-vs-after-fetch, affiliate URL and rel. Render compare-people-data-providers.md's two tables, bring-your-own-people-provider.md's list, and the LeadMagic-only phone caveat on find-phone-numbers.md:12 from it via Liquid. Same treatment for _data/integrations.yml driving integrations.md's list and section order. [content-ops/M/stretch]
- (idea by coach) Glossary data file that defines terms in place, on every page — Build _data/glossary.yml for signal, search, filter, persona, lead list, project, credit, personation, provenance envelope, as_of, X-Meter-Class, Tier 0/Tier 1, at-source vs after-fetch — plus a Liquid include that wraps first use on any page in a dotted-underline term with the definition inline, and renders a /glossary page from the same file. [onboarding/M/stretch]
- (idea by coach) Turn ci.yml from one build step into a real quality gate — Extend .github/workflows/ci.yml (today a single `bundle exec jekyll build`) with html-proofer for links, an alt-text check that fails on any `alt=""`, a `last_verified:` front-matter check that fails any page older than 180 days, and an orphan check that fails any page with zero in-body inbound links. Render last_verified as a visible date badge in the page header. [content-ops/M/stretch]
- (idea by coach) Trust center with a sub-processor table nobody has to maintain — Build /security rendering the sub-processor table from _data/providers.yml and _data/integrations.yml (the ~17 third parties the docs already name), plus the security facts currently stranded as feature copy: one-time key display and hash-only storage (agent-data-plane.md:121), Bearer auth and 403/404 semantics (api-access.md:36-44), secret-manager guidance for plane_api_key (agent-data-plane-mcp.md:30-35). Add a downloadable DPA and /.well-known/security.txt. [trust/L/stretch]
- (idea by coach) Explode the changelog into entries that feature pages subscribe to — Split whats-new/index.md's 105 entries into a Jekyll collection, one file each, with `feature:` and `date:` front matter. Render the changelog page from it, and render a "Recent changes" block on each feature page from the matching entries — so Recruit CRM (whats-new/index.md:25) appears on integrations.md automatically and the dedup options appear on remove-duplicate-signals.md. Add a "what changed since" date picker. [content-ops/L/stretch]
- (idea by coach) A provider-spend calculator on the credits page — Build /credits with the missing billing facts (what consumes a credit, per-plan allowance, 60-day expiry, free-tier limits) plus a small self-contained JS calculator fed by _data/providers.yml: pick companies per month, decision-makers per company, provider, and phones on/off, and it estimates provider spend using each provider's at-source-vs-after-fetch profile from compare-people-data-providers.md. [conversion/M/stretch]
- (idea by coach) Copy-paste agent recipes: MCP config, curl-to-CSV, Clay column blob — One page of paste-ready artifacts: the JSON MCP server block for `signalsapi-plane` with `plane_api_key` wiring, a curl piped to jq that pulls /projects/{id}/leads/export?format=csv into a file, a Clay HTTP-enrichment column config matching agent-data-plane-clay.md, and a sample webhook payload matching integrations.md's exact 20-field Clay JSON for n8n/Make users. [developer-experience/M/stretch]
- (idea by coach) Three role-shaped quickstarts, not one generic getting-started — Write three first-20-minutes paths ending in a real app URL: recruitment agency owner (create a search → Exclude staffing agencies → connect provider key → Bullhorn destination), B2B service provider (ICP filters → Personas → AI filter prompt → Instantly), and agent builder (Settings → API Key → GET /projects/ → export CSV). Each names the literal buttons and links to the deeper reference pages it touches. [onboarding/L/stretch]
- (idea by coach) Promote Hard to fill and Hiring surge into standalone concept pages — advanced-search.md:83-89 defines two proprietary, quantified concepts in passing — Hard to fill (open 30+ days or reposted 3+ times) and Hiring surge (5+ new roles in 30 days at 1.5x the prior rate) — plus the Exclude staffing agencies filter at :70. Give each its own page that defines the concept, explains why it predicts responsiveness, shows the signal shape, and links back into advanced-search and a search-creation quickstart. [seo/M/stretch]
- (idea by coach) Publish a data-subject page: "Why am I in SignalsAPI?" — A public page written for the decision-makers whose emails, LinkedIn URLs and mobile numbers the product resolves: what data is held, that it came from their employer's job posting plus a named people-data provider, the lawful basis, and a self-serve removal form that writes into the global do-not-contact suppression list. Link it from privacy-policy.md and /security, and reference it as the sender's compliance answer. [trust/M/wild]
- (idea by coach) Publish how SignalsAPI makes money, affiliate links included — A /how-we-make-money page stating plainly: SignalsAPI charges no credits on people lookups (compare-people-data-providers.md:16), we earn referral fees from Anymail Finder, Prospeo and Snov.io — the 22 `?via=signalsapi` / `?fp_ref=signalsapi` links across two pages — and here is why bring-your-own is still cheaper than a bundled reseller margin. Add rel="sponsored nofollow" to every one of them and a one-line disclosure on both provider pages. [trust/S/wild]
- (idea by coach) Publish the docs' own health scorecard at /docs-health — A CI-generated page showing this site's live quality metrics: pages missing a description, images with empty alt (currently 62 of 70), broken internal links, pages past their last_verified date, orphan pages with no inbound links, and the count of screenshots older than the current app UI. Regenerate it on every build and let the numbers go down in public. [trust/M/wild]
- (idea by coach) Delete nine pages: cut the site from 28 to 19 — Retire is-it-working.md (23 words duplicating faq.md:77), nav_exclude request-new-feature.md, and fold the seven GitBook-era pages whose 2024-10 screenshots contradict the current app — find-decision-makers, do-not-contact-list, remove-duplicate-signals, filter-leads-with-ai, personalize-emails-with-ai, location-based-filtering, website-monitoring — into two accurate, freshly-screenshotted pages: "Build your lead list" and "Write with AI". Redirect the old URLs. [content-ops/L/wild]
- (idea by coach) Replace "email us" with a public build log for the agent data plane — Restructure the four agent-data-plane pages under a /preview/ section fronted by a build log updated weekly: what shipped against the spec, what is next, and the date the base URL lands — plus a waitlist form capturing usage shape (companies per month, max_age needs, Tier 0 vs Tier 1) and indicative per-unit pricing for the four meter units (call, change, watch, forced_fresh). Fix the mcp.md:26 and :83 "works today" claims and state that 4 of 6 primitives are REST-only. [conversion/M/wild]
- (technique by coach) started Trickster's Gambit
- (idea by coach) Publish the hiring ledger as public market pages — Generate hundreds of static pages from GET /v1/market/role-demand and /v1/reqs/search (agent-data-plane-api.md:282-333) into _data/role-demand.json, rendering /hiring-demand/{role}-{country}/ pages — e.g. 'QA Engineer hiring demand, United Kingdom' — each with a 12-month series, current open-req count, and the repost-pain leaderboard from agent-data-plane-api.md:238. Regenerate monthly in a scheduled GitHub Action alongside the existing pages.yml build. [seo/L/wild]
- (idea by coach) Ship /llms.txt and a signalsapi-docs MCP server — Commit /llms.txt and /llms-full.txt (a single flat concatenation of all 28 pages) and publish a tiny read-only MCP server, signalsapi-docs, that serves those pages as tools. The plane's own MCP server is blocked on hosting (agent-data-plane-mcp.md:20) — a docs MCP has no such dependency and ships in a day. [developer-experience/M/wild]
- (idea by coach) Ship the spec and a mock server for the unbuilt plane — Commit /openapi/plane-v1.yaml covering every endpoint agent-data-plane-api.md already documents, plus the MCP tool manifest from its eight-tool table, plus a one-command mock (`prism mock openapi/plane-v1.yaml`) with the exact example payloads from agent-data-plane-clay.md. Replace 'email us for a base URL' with 'build against the mock now, swap the URL later'. [developer-experience/M/wild]
- (idea by coach) Write the page titled "When not to use SignalsAPI" — A top-level anti-sell page listing the real disqualifiers already scattered through the repo: no people data without your own provider key (bring-your-own-people-provider.md:15), mobile numbers only via LeadMagic (find-phone-numbers.md:12), no native Salesforce despite faq.md:73, Recruit CRM pushes contacts not candidates (whats-new:25), export rate limits unenforced (api-access.md:191), agent plane not self-serve. [trust/S/wild]
- (idea by coach) Document how to leave: export, cancel, keep your data — New /exporting-your-data page: the full-project export endpoint and its exact CSV columns (api-access.md:147-177), how to pull every project before cancelling, what happens to unspent credits (whats-new:53 — they expire in 60 days), and how to remove a provider key (bring-your-own-people-provider.md 'Removing or switching'). [trust/S/stretch]
- (idea by coach) Name doc URLs after symptoms, not features — New /errors/ section with one short page per real failure, each URL matching what a stuck user types: /errors/no-leads-found, /errors/phone-column-empty, /errors/provider-key-invalid, /errors/api-403, /errors/clay-domain-not-found. Each restates the answer currently buried as an aside (bring-your-own-people-provider.md:15, find-phone-numbers.md:12, api-access.md:40-46, agent-data-plane-clay.md 'When a company doesn't resolve') and links the owning page. [support-deflection/M/safe]
- (idea by coach) Turn 22 affiliate links into a disclosed cost calculator — Rebuild compare-people-data-providers.md around an inline JS calculator: enter leads/month, number of target titles and countries, and it computes estimated provider spend per provider using the at-source vs after-fetch matrix already on lines 51-60. Add rel="sponsored nofollow" and a one-line disclosure at the top — then let the calculator recommend Hunter or PDL even where no referral fee exists. [seo/M/stretch]
- (idea by coach) Drop a real 500-row sample export on the docs site — Commit /samples/leads-sample.csv with the exact column set api-access.md:172 emits (website, decision_maker_linkedin_url, decision_maker_email, decision_maker_phone, signal_job_title) using real companies and redacted person fields, plus /samples/clay-enrich-response.json matching agent-data-plane-clay.md's schema. Link both from index.md, api-access.md and agent-data-plane-clay.md. [conversion/S/stretch]
- (idea by coach) Stamp every page with a verified date and list the rot — Add `verified: YYYY-MM-DD` to every page's front matter, render it as a small badge in the theme layout, and add /stale which lists any page whose stamp is over 180 days old. Eight pages would immediately show 2024-10-22 — find-decision-makers.md, remove-duplicate-signals.md, location-based-filtering.md, website-monitoring.md, do-not-contact-list.md, filter-leads-with-ai.md and friends. [content-ops/S/safe]
- (idea by coach) Split Clay into two job-named pages plus a playbook — Rename integrations.md's Clay section to clay-push-leads.md and agent-data-plane-clay.md to clay-enrich-companies.md, each opening with a one-line disambiguation pointing at the other (copy api-access.md:18-20's pattern). Then add clay-hiring-surge-playbook.md — a full Clay recipe using the is_surge column to trigger outreach. [seo/M/stretch]
- (idea by coach) Explode the changelog into dated pages with RSS — Split whats-new/index.md (729 lines, 53 screenshots, dead since 18.06.2026) into per-release pages under dated URLs, add jekyll-feed for /feed.xml, and backfill the July 2026 entries the changelog is missing entirely — API access and the agent data plane, both committed 2026-07-09 with no announcement. [content-ops/M/stretch]
- (idea by coach) Publish the support inbox weekly as "Asked this week" — A running /asked page, appended every Friday, with the verbatim questions that hit mykola@signalsapi.com plus the answer and a link to the owning page. Seed it with the four known highest-volume ones: why a new account returns no people, why the phone checkbox does nothing, what consumes a credit, how often searches run. [support-deflection/S/wild]
- (idea by coach) Make the 404 the site's highest-intent router — Add 404.md with `permalink: /404.html` and the theme layout, containing the search box, six task-named destinations (create a search, connect a provider, integrations, API access, agent data plane, pricing), and both an 'Open app' and a 'Start free' link. Today there is no 404 at all, so every stale or mistyped URL lands on GitHub's unbranded page. [onboarding/S/safe]
- (idea by coach) Uncomment aux_links; put a way in above the pitch — _config.yml lines 9-11 hold a commented-out aux_links block pointing at the just-the-docs template repo. Replace it with Open app / Start free / Pricing / Status, and add one line at the top of index.md above 'Why SignalsAPI?' offering the free tier (company signals with no provider key, per bring-your-own-people-provider.md:15) as a self-serve alternative to signals.fillout.com/meet. [conversion/S/safe]
- (idea by coach) Mirror the app's own screen names in the sidebar — Retire 'Features' as a nav noun and group pages under the literal screens the docs already deep-link into — Searches, Filters, Personas, Lead lists, Settings — the same five that location-based-filtering.md:16-18 and filter-leads-with-ai.md:30 send users to. Add a 'Screen → doc' table on the new landing page mapping each app screen to its page. [onboarding/M/stretch]
- (idea by coach) A pricing page that tells you not to buy yet — New /pricing replacing faq.md:26 and :51 as the pricing source of truth: plan table, what consumes a credit, the 60-day expiry (whats-new:53), the 150-credit free allowance (whats-new:639), and a JS simulator where signals/month and personation rate produce an estimated credit burn — explicitly telling low-volume visitors the free tier is enough for now. [conversion/M/stretch]
- (idea by coach) Sell the BYO model as a privacy advantage on a /security page — New /security page stating the structural fact nobody has noticed: because customers bring their own provider key (bring-your-own-people-provider.md), SignalsAPI never resells people data and is not the source of the personal data in your pipeline — your provider is. Add the encrypted-at-rest key handling, a named sub-processor list, and 'DPA on request'; link it from privacy-policy.md, which is still dated January 22, 2025. [trust/M/stretch]
- (idea by coach) Let readers maintain the docs; publish the roadmap gaps — Enable gh_edit_link in _config.yml so every page gets 'Edit this page on GitHub', retire features/request-new-feature.md's bare Featurebase link into the footer, and add /roadmap listing the plane's own stated gaps verbatim — MCP not hosted (agent-data-plane-mcp.md:20), no public base URL (agent-data-plane.md:113), export rate limiting unenforced (api-access.md:191) — each with a one-click 'I'm waiting on this'. [content-ops/S/wild]
- (idea by coach) Answer-first rewrite plus FAQPage JSON-LD across the FAQ — faq.md is already 16 H2 questions — emit schema.org FAQPage JSON-LD from them via head_custom.html, rewrite each answer to open with one extractable sentence, and give all 28 pages a unique front-matter description (every page currently ships the identical 'SignalsAPI documentation'). Fix the three answers that self-sabotage while you are in there: '100% reliable', 'real-time', and the 'active development stage' line. [seo/S/safe]
- (idea by coach) List all 290 sources you claim to monitor — New /sources page with the full table — board or source name, country, coverage type, date added — behind the '290+ data sources' claim in index.md:17 and the '150+ job boards' claim in faq.md:11. The changelog already names nine by accident (TechInAsia, Reed, Justjoin, Jooble, Finn.no, Jobindex, Monster, RocketJobs, HiringCafe); publish the rest and keep it appended from the changelog. [seo/M/stretch]
- (technique by coach) started Post-Scarcity Test
- (idea by coach) Providers become _data/providers.yml; five tables render from it — Move the eight-provider truth (mobile support, at-source vs after-fetch per filter, credential shape, signup URL, affiliate flag) into `_data/providers.yml`, then render compare-people-data-providers.md's two tables, bring-your-own-people-provider.md's provider table, find-phone-numbers.md's LeadMagic-only claim, and a new prerequisite column on ai-variables.md via Liquid includes. The affiliate flag drives `rel="sponsored nofollow"` and the disclosure line automatically wherever a `?via=`/`?fp_ref=` URL is emitted. [content-ops/M/safe]
- (idea by coach) Vale vocabulary in ci.yml bans personation, lead list, 100% — Add a Vale step to the existing build job in .github/workflows/ci.yml with a SignalsAPI vocabulary: deny `personation`, deny `lead list`/`persona` where `project` is canonical, deny `mykola@signalsapi.com` outside a contributor file, deny unqualifiable absolutes (`100% reliable`, `real-time` without a stated floor), deny `Salesforce` until integrations.md documents it, and require every new .md to carry a non-default `description:`. [content-ops/S/safe]
- (idea by coach) Ship /llms.txt and /llms-full.txt as build artifacts — Generate `llms.txt` (titled, one-line-described index of all 28 pages grouped Getting started / Search & filter / People data / Integrations / APIs) and `llms-full.txt` (every page's Markdown concatenated) from `site.pages` in a Liquid template with `layout: null`, plus add jekyll-sitemap and a robots.txt pointing at both. [seo/S/safe]
- (idea by coach) 'No leads appeared' branching diagnostic from _data/symptoms.yml — Author a `_data/symptoms.yml` decision tree keyed to the four real causes the docs already contain as asides — no provider key so the account is on the free tier (bring-your-own-people-provider.md:14), phone column empty because the provider is not LeadMagic (find-phone-numbers.md:12), credits exhausted or silently expired at 60 days (whats-new/index.md:53), and run cadence gated by plan (whats-new/index.md:612) — then render it into a Troubleshooting page, into faq.md, and into 404.md from that one file. [support-deflection/S/safe]
- (idea by coach) Nine integration pages generated from _data/integrations.yml — Replace the single integrations.md — whose numbered list omits Zoho and Firefish, includes Instantly and Webhook with no setup, and misses Recruit CRM entirely — with `_data/integrations.yml` (name, credentials needed, plan requirement, callback URL, field mapping, setup steps, video) driving one child page per integration under an Integrations section, plus a generated index table. [seo/M/safe]
- (idea by coach) Pricing page computed from _data/plans.yml plus credit calculator — Create `_data/plans.yml` (plan name, price in £/€/$, included credits, run frequency, Tier 0/Tier 1 access, phone availability) and render a /pricing page from it, with a JS calculator that takes searches-per-week and leads-per-month and returns both SignalsAPI credits AND the reader's expected provider bill using the per-provider at-source/after-fetch data from `_data/providers.yml`. Point tos.md §5 and faq.md at it and delete the stale £400/£49-£199 prose. [conversion/M/safe]
- (idea by coach) Nightly Playwright rig re-shoots all 17 PNGs, stamps date — Write a `screenshots.spec.ts` that signs a seeded demo tenant into app.signalsapi.com and captures every documented surface — Settings→Provider (4 states), Personas, Filters, Lead Lists, dedup controls, phone toggle — writing each PNG plus its alt text from a spec map; run it nightly in ci.yml, commit on pixel diff, and render `Screenshot taken {date}` under every `<figure>` from file mtime. [trust/L/stretch]
- (idea by coach) Nightly contract test executes every curl block in api-access.md — Add a job that parses the fenced `bash` blocks out of features/api-access.md and features/agent-data-plane-api.md, runs each against api.signalsapi.com with a CI-held test key, and diffs the JSON keys against the adjacent `json` block — failing the build when the API drifts from the page. Render a generated 'Examples verified against production on {date}' line at the top of both references. [developer-experience/M/stretch]
- (idea by coach) Generate both API references from the served OpenAPI spec — Emit OpenAPI from the FastAPI apps behind api.signalsapi.com and the `/v1` plane, commit them to `_data/openapi-app.json` and `_data/openapi-plane.json`, and generate the endpoint tables, parameter tables, and the agent-data-plane-mcp.md tool-parity table from those specs — replacing the 8 hand-typed rows that currently back a 'can never drift' claim while 4 of the 6 advertised primitives have no MCP tool. [developer-experience/L/stretch]
- (idea by coach) One file per changelog entry; feature pages pull their history — Split the 729-line whats-new/index.md into a `_whatsnew/` collection, one file per dated entry with `feature:` front matter naming the page it affects; render the chronological changelog from the collection, and render a 'What changed on this page' block on each feature page from the same data. A CI rule fails any entry with no `feature:` target, forcing Recruit CRM, the status badges, and the 60-day credit expiry into real pages. [content-ops/M/stretch]
- (idea by coach) Front-matter verified: date rendered as public freshness badge — Add `verified: 2026-07-31` and `verified_against: app@<sha>` to every page's front matter, render it as a badge in the theme's page header, and fail CI on any page older than 90 days. Mirror the plane's own vocabulary — the badge is the docs' `as_of`, exactly what agent-data-plane.md:60 promises the data carries. [trust/S/stretch]
- (idea by coach) ?provider=leadmagic rewrites every provider-conditional sentence site-wide — Once provider facts live in `_data/providers.yml`, wrap every provider-conditional sentence in `data-when-provider` attributes and add a persistent provider picker in the nav; `?provider=leadmagic` then makes find-phone-numbers.md say 'your provider supports mobile' rather than 'currently LeadMagic only', greys the at-source filters Hunter cannot do, and shows Snov.io readers the two-secret form. Purely static, resolved client-side. [onboarding/M/stretch]
- (idea by coach) Search recipes: copy-paste search configs per job family — Publish a Search recipes section: one page per job family × top country (Construction PM / UK, QA Engineering / DE, Finance / US …), each carrying a ready-made advanced-search config — keyword lines, semantic phrase, job-title required words, description required words for remote, stop words, skills, company size, Exclude staffing agencies, Hard to fill — as a copy-paste block plus a deep link into app.signalsapi.com that prefills the search form. [onboarding/M/stretch]
- (idea by coach) A page per monitored job board — all 150+ — Build `_data/sources.yml` from the crawler registry (board name, country, crawl cadence, fields returned, whether descriptions are captured, first-observed date) and generate one page per source plus a searchable index, replacing the unlinked '290+ sources / 150+ job boards' claim that appears in index.md:17 and faq.md:11 with a verifiable directory. [seo/L/stretch]
- (idea by coach) Log zero-result site searches; agent writes missing page weekly — Instrument the just-the-docs lunr search to POST query + result-count to a collector, then run a weekly job that clusters zero-result and single-result queries and opens a PR with a stub page or FAQ entry per cluster. Seed the collector's first week from the known gaps: 'credits', 'Salesforce', 'Recruit CRM', 'sign up', 'pricing', 'API key'. [content-ops/M/stretch]
- (idea by coach) Localize the top five pages into the job sources' languages — Translate index.md, how-it-works.md, advanced-search.md, bring-your-own-people-provider.md and integrations.md into German, Dutch, Norwegian, Danish and Polish — the markets behind the sources the changelog names (Jobindex, Finn.no, Justjoin) — with proper hreflang and a locale switcher, generated and re-generated by agents on every English change. [seo/L/stretch]
- (idea by coach) Publish a nightly eight-provider match-rate benchmark — Run all eight connected providers nightly over the same fixed 500-domain panel and publish match rate, verified-email rate, mobile-hit rate (LeadMagic), headline coverage, median latency and cost-per-verified-contact as a dated public page fed by `_data/benchmark.yml` — with the affiliate relationship disclosed in the same breath. [seo/L/wild]
- (idea by coach) Hiring Pulse Index: live market page fed by /v1 — Add a GitHub Actions cron that queries the plane's market role-demand and change-feed endpoints with a build-time key, writes `_data/live.yml`, and renders a public Hiring Pulse Index — requisitions observed in the last 24h, top movers by role and country, repost-pain leaders, ATS migration counts — with every figure carrying its `as_of` exactly as the API does. Feed the same numbers into index.md's opening line. [trust/L/wild]
- (idea by coach) Public sandbox key for Tier 0, with Run buttons — Issue a public, heavily-rate-limited sandbox key scoped to Tier 0 cached reads over a fixed demo-company set, publish the base URL, and put a Run button on every example in features/agent-data-plane-api.md that fetches live and shows the real response with its `X-Data-Freshness` header. Replace the four 'email us for a base URL' dead ends with it. [conversion/L/wild]
- (idea by coach) Host the docs themselves as an MCP server — Ship an MCP server exposing the docs corpus as tools — `search_docs`, `get_page`, `list_integrations`, `compare_providers`, `explain_ai_variable` — built from the same `_data/*.yml` registries, and document connecting it in one page. A company selling an MCP server for hiring data ships an MCP server for its own manual. [developer-experience/M/wild]
- (idea by coach) Publish 'When SignalsAPI is the wrong tool' — Write an honest anti-sales set: a page naming the cases where SignalsAPI loses (you need candidate sourcing not client sourcing; you only sell in one metro with 20 target accounts; you need Salesforce today; you want people data with no provider account), plus straight comparisons against job-board alerts, Clay-only stacks, and LinkedIn Sales Navigator — each stating what the alternative does better. [trust/M/wild]
- (idea by coach) Publish the docs-coverage gap table, live — Build `_data/surfaces.yml` enumerating every user-facing surface in the app (each search field, each filter, each persona setting, each integration, each API route, credits, plans) with the page that documents it, render it as a public coverage table with an explicit percentage and a list of undocumented surfaces, and fail CI when coverage drops. [content-ops/M/wild]
- (technique by coach) started Backcasting
- (idea by coach) Public sandbox base URL + self-issued keys for the agent data plane — Stand up sandbox.api.signalsapi.com/v1 seeded with a few hundred anonymized companies, and replace the "email mykola@signalsapi.com" step at features/agent-data-plane.md:113-117, agent-data-plane-api.md:15 and agent-data-plane-clay.md:42 with a self-issue form that mints a read-only sandbox key on the spot. Every endpoint block in agent-data-plane-api.md gets a copy-paste curl that works against the sandbox without an account. [conversion/L/wild]
- (idea by coach) Generate 4,000 /market/ pages nightly from the hiring-events ledger — Add a GitHub Action that calls the plane's role-demand primitive nightly, writes _data/market/*.json, and renders a /market/{role}-{country} page per cell (e.g. /market/qa-engineer-united-kingdom): 12-month demand trend, count of companies with repost pain, top hiring functions, each linking to /glossary and /quickstart. The site's own product writes its own SEO surface. [seo/L/wild]
- (idea by coach) Serve the docs themselves over MCP, plus llms.txt and openapi.yaml — Publish /llms.txt and /llms-full.txt, commit an openapi.yaml covering api.signalsapi.com's three routes from features/api-access.md and the /v1 plane routes from agent-data-plane-api.md, and ship a `signalsapi-docs` MCP server exposing a docs_search tool. Link all three from a new /apis/ page. [developer-experience/M/wild]
- (idea by coach) Delete the pitch: turn index.md into a six-tile task launcher — Strip index.md down to three sentences of what SignalsAPI is and six links — Start free, Quickstart, Core concepts, Integrations, APIs, Pricing & credits. Move "Which side do you want to be on in 12 months?" (index.md:26) and the fillout.com/meet booking (index.md:28) to signalsapi.com. Also remove is-it-working.md and drop request-new-feature.md (nav_order 0) out of the Features nav. [conversion/S/wild]
- (idea by coach) "Why am I getting zero leads?" decision tree — New page troubleshooting/no-leads.md branching through the six real causes, each with the symptom the user sees: no provider key → free tier, company signals only (bring-your-own-people-provider.md:14); provider key shows invalid; Phone column empty because LeadMagic is the only mobile provider (find-phone-numbers.md:12); "Companies can be matched again" set to Among all lists; credits at 0 with 60-day expiry; run frequency capped by plan. [support-deflection/S/safe]
- (idea by coach) Statuses & error codes reference — the page users search when stuck — One page collecting every machine-facing signal the product emits: 403/404/422 from api-access.md:40-45, the 202 {"status":"crawling"} response and X-Data-Freshness / X-Meter-Class headers from agent-data-plane.md:66-98, and the app's own status badges (Searching, Enriching…, Leads found, Qualified) that exist only in whats-new/index.md:15-21. [support-deflection/S/safe]
- (idea by coach) One /apis/ router page that ends the Clay confusion for good — Create apis/index.md with a four-row "what do you want" table: get my leads out → API access; query market hiring history → agent data plane REST; give an agent tools → MCP; enrich a Clay table by domain → agent-data-plane-clay.md; push finished leads into a Clay table → integrations.md §Integrating with Clay. Add a one-line disambiguation banner at the top of both Clay pages pointing at each other. [onboarding/S/safe]
- (idea by coach) Redirect every GitBook-era URL instead of 404ing it — Add jekyll-redirect-from, then map the old GitBook paths (the site's whole content history is a GitBook export — escaped underscores in ai-variables.md, `"mention"` link artifacts in filter-leads-with-ai.md:32, `\` hard breaks in how-it-works.md) onto their current pages, and give features/ai-variables the alias the dead link points at. [seo/S/safe]
- (idea by coach) Support page with a copy-paste diagnostic block — One support.md replacing the split between mykola@signalsapi.com (faq.md:81, filter-leads-with-ai.md:26/:46, agent-data-plane.md:116) and support@signalsapi.com (privacy-policy.md, tos.md): single address, hours, response target, status.signalsapi.com link, and a fenced block listing exactly what to include — project id, provider name and key status, search name, run timestamp, and X-Data-Freshness if it is an API issue. [support-deflection/S/safe]
- (idea by coach) Run every curl example in api-access.md nightly in CI — Add a scheduled workflow that executes the three documented calls — GET /projects/, GET /projects/{id}/leads, GET /projects/{id}/leads/export?format=csv — against a demo account, asserts the documented JSON keys (usable_lead_count, person.headline, total) and the documented CSV header `website,decision_maker_linkedin_url,decision_maker_email,decision_maker_phone,signal_job_title`, and fails loudly when the API drifts. [developer-experience/M/stretch]
- (idea by coach) Let the app generate its own screenshots weekly — A Playwright job against a seeded demo tenant that re-captures the eight 2024-10 PNGs (find-decision-makers-1/2, filter-leads-with-ai, do-not-contact-list, remove-duplicate-signals-1/2, personalize-emails-with-ai-1/2/3) at fixed viewport and commits them with descriptive alt text pulled from a captions file. Fail the build if any screenshot's app build hash differs from the current release. [content-ops/M/stretch]
- (idea by coach) Provider picker + cost-per-usable-lead column on the comparison page — Add a small inline picker to features/compare-people-data-providers.md — need mobile? filter by city? need LinkedIn headlines? domain-only companies? — that resolves to one of the eight providers using the at-source/after-fetch matrix already on the page, plus a computed "est. provider cost per usable lead" column. Add the missing affiliate disclosure and rel="sponsored" on the 22 ?via= / ?fp_ref= links. [conversion/M/stretch]
- (idea by coach) Credits & spend estimator on a real Pricing page — Create pricing.md (tos.md §5 already cites a pricing page that does not exist) with the plan table, the free-tier limit, the 60-day credit expiry from whats-new/index.md:53, and an estimator: searches × run frequency × decision-makers per company × your provider's per-lookup price → monthly provider bill and SignalsAPI credit burn. Fix faq.md:51's £49/£149/£199 phone pricing, which contradicts the BYO-LeadMagic model. [conversion/M/stretch]
- (idea by coach) Own the vocabulary: a glossary page per coined term — One page each for repost pain, hiring pulse, first hire, ATS migration, reobserved vs reposted, hard-to-fill and hiring surge — terms SignalsAPI invented in agent-data-plane.md:30-51 and advanced-search.md:83-89. Each defines the term, shows the event shape or the filter that measures it, and links to the primitive and the search setting that produce it. [seo/M/stretch]
- (idea by coach) A /recipes/ section of end-to-end plays, not feature descriptions — Eight recipes that cross pages, each naming real settings and endpoints: "Pitch companies that failed to fill a role three times" (Hard to fill + repost-pain + Persona + Bullhorn destination); "Score inbound domains in Clay with hiring pulse"; "Create a HubSpot deal when a watched account opens its first-ever engineering req" (watch_company + webhook); "Remote-only UK QA leads" (semantic search + description required words remote/home/hybrid + Exclude staffing agencies). [conversion/L/stretch]
- (idea by coach) Publish a last_verified date on every page and gate it in CI — Add `last_verified:` to every page's front matter, render it in the page header ("Verified 2026-07-31 against app build 4.2"), and fail the build when any page passes 180 days unverified. Seed it honestly: privacy-policy.md would show January 2025, the eight legacy screenshots October 2024. [trust/M/stretch]
- (idea by coach) Split the changelog into a collection and make it block releases — Convert whats-new/index.md's 105 entries into a _changelog collection, one page per dated entry with its own screenshot, an RSS feed, and a required `read_more:` key. CI fails when an entry tagged as a feature has no target page. Backfill the changelog-only features first: Recruit CRM (:25), the status badges (:15-21), credit expiry (:53), run-frequency-by-plan (:612). [content-ops/M/stretch]
- (idea by coach) Trust center that unblocks procurement without a call — One /trust/ page naming the eight people-data providers as sub-processors, the customer-credential handling already claimed at bring-your-own-people-provider.md:89-92, a downloadable DPA, the status.signalsapi.com embed, and a re-dated privacy policy that actually covers the decision-maker names, emails, mobiles and LinkedIn URLs the product processes. [trust/M/stretch]
- (idea by coach) Instrument the lunr search box and publish the zero-result report monthly — Wire the just-the-docs search input to fire an analytics event per query, add a "nothing found — email us or tell us what you expected" fallback in the empty state, and publish a monthly list of top zero-result queries that becomes the content backlog. Pair it with tagging every support reply with the page that should have answered it. [content-ops/S/stretch]
- (idea by coach) Route the home page by audience, not by feature — Three hub pages behind the launcher tiles: "Recruitment agency owner" (quickstart → advanced search → integrations → pricing), "RevOps / Clay & CRM" (integrations → Clay enrich → AI variables → webhook fields), "Developer / agent builder" (apis router → plane REST → MCP → sandbox key). Each hub is 150 words and five links; features/index.md becomes the grouped index behind them. [onboarding/M/stretch]
- (idea by coach) Publish plane pricing and rate limits before the endpoint exists — Replace "Pricing is not yet published; talk to us" (agent-data-plane.md:106) with an indicative Tier 0 / Tier 1 price per call, stated rate limits per meter unit (call, change, watch, forced_fresh), and a waitlist form asking usage shape — companies watched, polls/day, max_age needed. Offer waitlist signups a price lock. Fix agent-data-plane-mcp.md:26 and :83, which promise REST "works today" against a surface with no public base URL. [conversion/M/stretch]
- (idea by coach) Monday's credibility patch: one PR, one hour, six defects gone — Single PR to _config.yml, ci.yml and two new files: `exclude: [README.md, LICENSE, push.sh, run.sh, Gemfile, Gemfile.lock]`; drop the trailing slash from `url: https://docs.signalsapi.com/` so 28 canonicals stop emitting a double slash; `permalink: pretty` so the 42 extensionless internal links match their targets; jekyll-sitemap plus robots.txt; a 404.md that runs the site's own lunr search against the mistyped path; aux_links uncommented with Open app / Start free / Status / Support; html-proofer added to ci.yml. [trust/S/safe]
- (technique by coach) started Completeness Critic
- (idea by coach) Per-page "Did this answer your question?" that files the miss — Add a two-button block to the theme layout (via a new _includes/feedback.html rendered by the just-the-docs page footer): No opens a one-field box that pre-fills a GitHub issue in the docs repo with the page slug, the H2 the reader was nearest, and their sentence; Yes just increments a count. No backend — the issue body is the log. [support-deflection/S/safe]
- (idea by coach) Record the baseline today, before any of the 266 ship — Commit _data/baseline.yml capturing 2026-07-31 as-is: 28 pages, 0 with a per-page description, 62/70 empty alts, 0 sitemap, 0 robots.txt, 17 pages with zero inbound links, last changelog entry 18.06.2026, tickets/week from the inbox, sessions and top landing pages from whatever analytics exists (none — so add a privacy-friendly script to _includes/head_custom.html in the same PR). Render it at /docs-baseline with a re-measure date. [content-ops/S/safe]
- (idea by coach) Deflection ledger: every ticket names the page that failed — Every answered email gets one line appended to _data/tickets.yml — date, one-line question, and the URL that should have deflected it (blank means the page does not exist yet). Render a monthly leaderboard of pages ranked by tickets-they-failed-to-deflect, and adopt a WIP rule: no new page is written while the top offender is unfixed. [support-deflection/M/stretch]
- (idea by coach) Make docs a merge gate in the app repo, not a follow-up — In the SignalsAPI application repo, add a PR template field "Docs PR or `docs: none`" enforced by a required check, plus an Action that opens an issue in the docs repo for every release-note bullet marked user-facing. Add CODEOWNERS to the docs repo mapping features/agent-data-plane*.md, features/integrations.md and features/advanced-search.md to named reviewers. [content-ops/M/stretch]
- (idea by coach) Watch five recruiters try, quarterly, and publish where they stall — Run a recorded 45-minute task test with five real agency users on five tasks: create your first search, connect a provider key at Settings → Provider, get one mobile number, push a lead into Clay, pull leads.csv from api.signalsapi.com. Score completion and the exact stall point; file each stall as an issue titled with the literal page and step. [onboarding/M/safe]
- (idea by coach) Generate an OG card and favicon for all 28 pages — Add a build step that renders a 1200x630 card per page (page title, its section, docs.signalsapi.com, the SignalsAPI mark on the signalsapi colour scheme from _sass/color_schemes/signalsapi.scss), writes them to /assets/og/, and wires og:image plus twitter:card into _includes/head_custom.html. Commit favicon.ico and the PNG set at the same time. [seo/S/safe]
- (idea by coach) Make it readable on a phone in a taxi — Add a mobile pass in _sass/custom/custom.scss: card-stack the 6-column at-source/after-fetch matrix in compare-people-data-providers.md and the endpoint tables in agent-data-plane-api.md below 640px, give code blocks horizontal scroll plus a copy button, cap the 3359px changelog PNGs with intrinsic width/height, and fix the *:focus rule to a solid focus-visible ring while in the file. [seo/S/safe]
- (idea by coach) Ship the docs into other people's directories — Build one purpose-made landing page per destination and submit listings: Clay templates gallery and HubSpot App Marketplace (pointing at features/integrations.md sections), Bullhorn and Zoho and Recruit CRM partner directories, MCP registries for the plane's server, and the Postman Public API Network for api.signalsapi.com's three routes. Each listing's "Documentation" field is a docs.signalsapi.com URL written for that audience. [seo/L/stretch]
- (idea by coach) Trade 22 outbound sponsored links for eight inbound ones — Write /partners/<provider> pages for the eight providers in bring-your-own-people-provider.md's table — what SignalsAPI does with that provider's key, which filters run at source, the exact credential fields — then ask each provider (Anymail Finder, Prospeo and Snov.io already pay referral fees) to list SignalsAPI in their integrations directory pointing at that page. [seo/M/stretch]
- (idea by coach) Serve the docs inside the app as a help panel — Publish docs.signalsapi.com/widget.js — a small self-contained script the app loads that renders a command palette over the existing lunr index (assets/js/search-data.json) and opens the right page in a side panel, keyed by screen: Settings → Provider, Personas, Filters, Lead Lists, Settings → Integrations, Settings → API Key. [support-deflection/L/wild]
- (idea by coach) The docs repo owns the app's empty-state and error strings — Add _data/ui-strings.yml holding the literal copy for the app's empty and blocked states — no provider key saved, Qualified with no decision-makers, 0 credits, key invalid, Manual Approval pending — each with its help URL; publish it as /ui-strings.json and have app.signalsapi.com render from it at build. [content-ops/L/wild]
- (idea by coach) Kill the subdomain: move everything to signalsapi.com/docs — Rehost the Jekyll build at signalsapi.com/docs/ behind the marketing site, 301 every docs.signalsapi.com URL (including the double-slash canonical form _config.yml:7 currently emits), and keep one redirect map covering the GitBook-era paths in the same change. [seo/L/wild]
- (idea by coach) Pay readers in credits to fix the docs — Enable gh_edit_link in _config.yml and GitHub Discussions on the repo, add a "Discuss this page" and "Fix this page" pair to the page footer, and publish a standing offer: an accepted docs PR or a confirmed inaccuracy report earns SignalsAPI credits, posted as a table of who fixed what. [content-ops/M/wild]
- (idea by coach) Answer "is it legal for me to email these people?" — New /outbound-compliance page for the customer, not the buyer's lawyer: legitimate interest for B2B contact data sourced from their own provider key, UK PECR and EU/CAN-SPAM differences, who is controller of the decision-maker record (them, not SignalsAPI — their key, their provider contract), what the do-not-contact list evidences, and the footer/opt-out language to use. [trust/M/stretch]
- (idea by coach) A prompt library with the outputs, including the empty ones — Publish ten proven prompts for filter-leads-with-ai.md and personalize-emails-with-ai.md — reject-if-agency, seniority check, first-line opener from {job_description}, phone-gated call script — each showing the literal prompt, a real input signal, the produced output, and what the same prompt returns on a Hunter key where {phone}, {phone_status} and {headline} come back empty. [onboarding/M/safe]
- (idea by coach) The operator's week: a one-page Monday-to-Friday SOP — Write /running-signalsapi-weekly for the person who logs in daily, not the person evaluating: Monday review signals stuck on the grey Qualified badge, Tuesday approve leads if Manual Approval is on (integrations.md:34-36) or they never reach the CRM, Wednesday check the credit counter and provider spend, Friday re-tune stop words from the week's rejects, monthly rotate the provider key. [onboarding/S/stretch]
- (idea by coach) Read the 53 screenshot filenames as a feature census — Turn whats-new/*.png filenames into a coverage table: 2024-04-04-daily-match-limit, 2024-04-16-control-runs-schedule, 2024-04-16-if-minimum-valid-emails, 2024-04-28-automatic-approval-and-rej, 2024-04-28-lead-settings, 2024-05-20-limit-the-number-of-dec, 2024-05-30-worldwide-search-on-linkedin, 2024-03-01-job-researcher-can-be-set, 2024-01-29-statistics-per-processor. Mark each shipped-and-documented, shipped-and-undocumented, or removed; the undocumented rows become the writing queue. [content-ops/S/safe]
- (idea by coach) Publish the outages, including the source ones — Add /incidents as a dated archive: docs and app downtime pulled from status.signalsapi.com, plus data-source incidents the status page cannot express — a board blocking the crawler, an ATS changing its schema, a provider API rejecting valid keys — each with duration, what customers saw (empty runs, stale signals), and what changed after. [trust/M/stretch]
- (idea by coach) Prove the site deployed: previews, smoke check, uptime — Extend .github/workflows/ci.yml past its single `bundle exec jekyll build` with a PR preview deployment, then a post-deploy smoke job asserting docs.signalsapi.com/features/api-access returns 200, /README.md returns 404, and the built site's newest page timestamp matches HEAD. Register docs.signalsapi.com as a component on status.signalsapi.com. [developer-experience/S/safe]
- (idea by coach) Nightly assertion that the app still has the buttons we name — Extract every app.signalsapi.com deep link and every bolded UI label the docs issue as an instruction — Settings → Provider, Save & validate, Remove key, Manual Approval, Approve, Uploaded, Download CSV, Settings → API Key, Settings → Integrations → Add Integration — into a checked inventory, and run a nightly Playwright job against a demo tenant that fails with the exact file and line when a label or route no longer exists. [developer-experience/M/stretch]
- (idea by coach) Publish an accessibility statement procurement can accept — Fix the *:focus contrast defect and the 62 empty alts, then publish /accessibility: conformance level claimed for both docs and app, known gaps with dates, keyboard-only path through the core workflow, and a contact for access requests. Offer it as a downloadable answer alongside the DPA. [trust/M/stretch]
- (idea by coach) Document the Virtual Networker handoff or stop selling it — index.md:13 and faq.md:29/47 push virtualnetworker.com three times with no page behind it. Either write one — which SignalsAPI object hands over (lead list or webhook destination), what the receiving side does with signal_title and company_linkedin, and who bills what — or remove all three mentions from the docs and leave them on signalsapi.com. [conversion/S/safe]
- (idea by coach) Every release ships as a page, a feed item, and a post — From the same release data that builds whats-new, render three outputs: the changelog page, /whats-new/feed.xml, and a ready-to-paste LinkedIn/email version of each entry with the Read more link already in it. Backfill the two 2026-07-09 launches — API access and the agent data plane — as the first posts. [content-ops/M/stretch]
- (technique by coach) started Cross-Pollination
- (idea by coach) Git log is the changelog — delete whats-new/index.md, compute it — Parents: #75 (releases.yml), #102 (per-page change strips), #211 (verified stamps). Instead of hand-maintaining 105 entries, a build step reads `git log --follow` per file, maps each commit to the page it touched, and renders both /whats-new (grouped by month, from `docs:`/`feat:` commit subjects) and a 'Changed 2026-07-09' stamp at the foot of every page. The 53 PNGs and the 1.9 MB payload go away entirely; whats-new/index.md is deleted. [content-ops/M/safe]
- (idea by coach) Help-anchor contract that reports back, and prunes pages that deflect nothing — Parents: #109 (help_anchors.yml), #112 (`deflects:` front matter), #37 (zero-result capture), #200 (docs-health). Publish /help-anchors.json mapping app screens to doc anchors; the app renders its '?' icons and empty states from it and posts an anchor-hit count back. /docs-health then publishes tickets-deflected-per-page, and CI fails any page under features/ whose `deflects:` list is empty AND whose anchors logged zero hits in 90 days. [support-deflection/M/stretch]
- (idea by coach) No affiliate link renders without its measured cost beside it — Parents: #50/#223 (_data/providers.yml), #28/#115 (spend calculator), #41/#199 (rel=sponsored + disclosure), #99 (eight-provider benchmark). One Liquid include emits every provider link, and it is physically incapable of emitting the URL without also emitting rel="sponsored nofollow" and a computed label: 'Hunter — title matched after fetch, est. +£38 per 1,000 leads vs People Data Labs'. Same data drives the two tables in compare-people-data-providers.md, the calculator, and the ninth row (SignalsAPI managed enrichment). [conversion/L/stretch]
- (idea by coach) The docs are the sandbox: frozen /v1 fixtures plus a service worker — Parents: #5 (public demo key), #205 (mock server), #21 (live reads in reference pages), #98 (seeded company set). Jekyll emits recorded responses at /fixtures/v1/... for ~50 real domains (zollsoft.de is already the worked example in agent-data-plane-clay.md and api-access.md). A service worker on the docs origin intercepts fetches to the documented base URL, so every curl block and every 'Run' button on agent-data-plane-api.md returns real-shaped JSON — X-Data-Freshness and X-Meter-Class included — with no backend, no key, and a visible 'recorded 2026-07-31' stamp. The same files are the `prism mock` target. [developer-experience/L/wild]
- (idea by coach) Two corpora, one domain, enforced by a page budget — Parents: #201 (cut 28 pages to 19), #246/#203 (generate thousands of market pages), #236 (a page per source). Split the site into /docs — hand-written, capped at 24 pages, where CI rejects any PR adding a page without retiring one — and /market, generated nightly from the ledger's role-demand primitive, nav_exclude, absent from lunr's search-data.json, with its own sitemap-market.xml. Publish the retirement ledger so every deleted page's URL and its replacement are visible. [seo/L/wild]
- (idea by coach) Search config as a URL — recipes are saved simulator states — Parents: #47 (search simulator), #105 (filter simulator), #235/#133 (search recipes), #175 (config builder). One page, /search-lab, implements the OR-within-field / AND-across-fields logic spelled out at advanced-search.md:42-46 over 15 baked signals, and encodes the whole config in the query string. The 12 vertical recipes become 12 committed URLs; each renders its literal field values (Search for, Job title required words, Stop words, Location patterns, Maximum age, Exclude staffing agencies, Hard to fill) plus a prefilled app.signalsapi.com link. [onboarding/M/stretch]
- (idea by coach) Docs that know your tier: signed ?ctx= from the app rewrites every page — Cranked from #73 (footer include) and #49/#234 (provider switcher). The app appends a signed, 15-minute-TTL `?ctx=` parameter to every help link carrying tier, connected provider, credit balance and phone-lookup flag. Docs render a state bar — 'Free tier · no provider key · 0 credits' — and resolve every conditional sentence: find-phone-numbers.md reads 'Your provider is Hunter. This page will do nothing until you connect LeadMagic'; ai-variables.md greys {phone}, {phone_status} and {headline}; compare-people-data-providers.md collapses to your row. [support-deflection/M/wild]
- (idea by coach) Make the limitations page the procurement artifact — DPA Annex B — Resolves #90/#182/#206 ('publish what we don't do') against #4/#147/#262 (trust pack that closes procurement). One dated, versioned 'Capability and limitations statement' where every row is limit + commitment + review date: no native Salesforce object (delete the promise at faq.md:73, state webhook-only), mobile numbers via LeadMagic only (find-phone-numbers.md:12), export rate limiting not yet enforced (api-access.md:191), Tier 0 reads deliberately freshness-floored (agent-data-plane.md:82-85), MCP server code-complete but unhosted. The same file is attached as Annex B of the DPA and re-dates privacy-policy.md off 'January 22, 2025'. [trust/M/stretch]
- (idea by coach) Docs MCP server whose tools also return live hiring data — Parents: #100/#180/#242 (docs as MCP server), #56 (free domain lookup), #31. Ship `signalsapi-docs` backed by the lunr index already built at _site/assets/js/search-data.json, exposing search_docs and get_page — then add one product tool, hiring_pulse(domain), served from the Tier 0 cached path. Document the connect snippet on features/agent-data-plane-mcp.md, immediately above the eight-tool table for the server that is 'code-complete but not yet deployed'. [developer-experience/M/wild]
- (idea by coach) Settings blocks that self-test — kill the screenshot instead of re-shooting it — Parents: #93 (copy-pasteable settings blocks), #62 (field reference tables), #167 (click paths), #36/#229 (Playwright capture). Declare each control once in _data/controls.yml — screen, click path, field label, options, default — and render three things from it: a values table, a breadcrumb (`Search → Duplicates → Companies can be matched again`), and an app deep link. A nightly Playwright job asserts each declared field still exists by its label and fails CI when the app renames it. remove-duplicate-signals-2.png and find-decision-makers-1/2.png become deletable. [content-ops/M/safe]
- (idea by coach) One price constant, five renderings, CI bans the rest — Parents: #72 (currency-symbol CI check), #131 (billing truth table include), #142 (worked baskets). _data/pricing.yml becomes the only place a £/€/$ figure, a credit allowance, the 60-day expiry or the 150 free credits exists. A `{% include price %}` renders it into /pricing, faq.md, tos.md §5 (which already cites a pricing page that 404s), bring-your-own-people-provider.md, find-phone-numbers.md and api-access.md. CI greps every .md for a bare currency symbol outside the data file and fails the build. [conversion/M/safe]
- (idea by coach) The pipeline is one SVG, rendered four ways — Parents: #58 ('you are here' strip), #77 (mermaid pipeline on how-it-works), #145 (printable cheatsheet). _data/pipeline.yml holds five nodes — Search → Filter → Persona → Lead list → Destination — each with its app URL, owning doc page, and prerequisite. From that one file render: the per-page orientation strip via a `stage:` front-matter key, the diagram that replaces how-it-works.md's twelve passive bullets, the printable /cheatsheet, and the 1200×630 og:image the site currently has no version of. [onboarding/S/safe]
- (idea by coach) Pages expire like food — unverified pages leave the index automatically — Cranked from #60/#211/#233 (verified_on front matter). Past `verified_on + 180d` a page renders an 'Unverified since 2024-10-22' banner, is dropped from sitemap.xml, removed from the lunr search-data.json, and served `noindex`. It stays reachable by direct link; it stops being findable. On day one the eight GitBook-era pages — find-decision-makers, remove-duplicate-signals, do-not-contact-list, filter-leads-with-ai, personalize-emails-with-ai, location-based-filtering, website-monitoring — leave search until someone re-verifies them. [trust/S/wild]
- (idea by coach) The app's string table becomes the docs search index — Cranked from #63 (search aliases in front matter). Export every literal string the app renders — the badges Searching… / Enriching… / Leads found / Qualified, 'Manual Approval', 'Companies can be matched again', 'available on paid plans', provider key active/invalid, 'Did you enjoy your trial? Time to upgrade!' — into _data/ui-strings.yml with an owning page and anchor. lunr indexes them as aliases, and CI fails when the app ships a user-visible string with no owner. [support-deflection/S/safe]
- (idea by coach) A waitlist that quotes itself before you press submit — Parents: #127 (qualifying waitlist), #265 (indicative Tier 0/Tier 1 pricing), #12 (meter units as a ledger). The access form on features/agent-data-plane.md takes companies watched, polls per day, whether max_age is needed, and Tier 0 vs Tier 1 — then runs the four documented meter units (call, change, watch, forced_fresh) client-side, including the rule that the change feed bills per event returned rather than per poll, and prints an indicative monthly figure with a price-lock offer before submission. [conversion/M/stretch]
- (idea by coach) Market pages ship the search config that reproduces them — Parents: #157/#246 (generated market pages), #235 (search recipes), #175 (config builder), #30 (quarterly reports). Every generated /market/{role}-{country} page ends with the exact advanced-search configuration that would surface the signals behind its leaderboard — literal values for semantic search, job-title required words, location patterns, Maximum age, Exclude staffing agencies and Hard to fill — as a copy-paste block plus a prefilled app link. Cells that convert get promoted into the hand-written /recipes library. [seo/L/stretch]
- (idea by coach) The removal form writes straight into the do-not-contact list — Parents: #198 (data-subject page), do-not-contact-list.md, #4/#262 (trust pack). Publish 'Why am I in SignalsAPI?' for the decision-makers whose emails, LinkedIn URLs and mobile numbers the product resolves: what is held, that it came from their employer's job posting plus a named people-data provider, the lawful basis — and a removal form that writes into the same global suppression list every customer's searches already honor. Publish the suppression count and the median honor time. [trust/M/wild]
- (idea by coach) The founder's inbox becomes a build input, not an address — Resolves #15/#214 (publish the inbox as docs) against #177/#253 (retire the personal inbox). mykola@signalsapi.com disappears from all nine customer-facing pages (faq.md:81/:85, filter-leads-with-ai.md:26/:46, integrations.md:24, the three agent-data-plane pages) in favour of one support address — while a mail rule files every answered thread as a draft PR against the page named in that page's `deflects:` front matter, using the customer's own question wording as the H2. [content-ops/M/stretch]
- (idea by coach) Contraindication boxes that resolve against your actual provider — Parents: #134 (PIL-style 'do not start unless' panel), #27/#163 (_data/prereqs.yml), #49 (provider switcher). One `_includes/prereq.html` fed by the prereqs data file renders the blunt negative above the fold on find-decision-makers.md, find-phone-numbers.md, filter-leads-with-ai.md, personalize-emails-with-ai.md and ai-variables.md. Once a provider is set (localStorage or ?provider=hunter) the box resolves: green 'LeadMagic connected — phone lookup available', or red 'Hunter connected — the Find phone numbers checkbox does nothing for you'. [onboarding/S/safe]
- (idea by coach) Router tiles carrying live numbers — pitch and proof in one component — Resolves #71/#94/#248 (strip index.md to a task router) against #169/#158 (home page proves the claim with live data). Six tiles replace index.md lines 11-28, each carrying one real number: 'Set up my first search — N signals written in the last 24h', 'Connect a people-data provider — 8 supported, you keep the key', 'Get leads into my CRM — 9 destinations', 'Call the API — 3 endpoints, spec at /openapi.yaml', 'Understand credits — 150 free', 'Something is broken — 6 symptom pages'. The Virtual Networker cross-sell and 'Which side do you want to be on in 12 months?' move to signalsapi.com. [conversion/M/stretch]
- (idea by coach) One spec, and the MCP parity claim becomes a computed sentence — Parents: #53/#110/#231 (OpenAPI as source), #26 (the parity finding), #128 (plane-status.json). Author openapi.yaml for both surfaces with two vendor extensions per operation: `x-mcp-tool` and `x-status: live|code-complete|planned`. Render agent-data-plane-api.md's endpoint list, agent-data-plane-mcp.md's eight-row tool table, and /plane-status.json from it — and replace the 'can never drift' parity language with a generated line: 'MCP exposes 8 of 14 operations; 6 are REST-only.' [developer-experience/M/stretch]
- (idea by coach) Publish the questions the site cannot answer, with counts — Cranked from #37/#263 (capture zero-result searches). Hook the lunr search box: zero-result queries POST to a collector and appear on a public /unanswered page with counts and an upvote link, refreshed weekly, under a stated promise that the top query each week becomes a page or an FAQ entry within seven days. Seed week one with the known gaps: credits, Salesforce, Recruit CRM, pricing, sign up, API key. [content-ops/S/safe]
- (insight by coach) SYNTHESIS 1 - Strongest convergence in the whole run: a public read-only sandbox/demo key for the agent data plane was independently invented by 8 of 12 techniques (JTBD, HMW, Assumption Reversal, Five Whys, SCAMPER, Trait Transfer, Kill the Crown Jewel, Post-Scarcity, Backcasting). When 8 unrelated lenses land on the same move, that is the answer, not a coincidence. It converts the one audience that buys without a human and it costs one cached Tier 0 read.
- (insight by coach) SYNTHESIS 2 - The master key is DRY applied to docs: move the six datasets (providers, ai-variables, integrations, glossary, plans/pricing, changelog) into _data/ and render pages as templates. This single structural move dissolves the 4-name object model, the faq-vs-phone-pricing contradiction, the integrations self-contradiction, the provider matrix duplication and changelog rot simultaneously. ~49 content-ops ideas are all facets of it.
- (insight by coach) SYNTHESIS 3 - llms.txt, an OpenAPI spec, and a docs MCP server are one idea wearing three hats: make the corpus machine-readable. The irony is the finding: a company selling an agent data plane and an MCP server publishes the least agent-readable docs on the internet - no sitemap, no robots.txt, 28 identical meta descriptions, 28 malformed canonicals. A docs MCP server has no hosting dependency and ships in a day, so it can be the plane's first live endpoint.
- (insight by coach) SYNTHESIS 4 - Counterintuitive theme that Trickster, Worst Possible Idea and Relativity Frame Shift all reached independently: admitting weakness is the conversion lever. When not to use SignalsAPI, published affiliate commissions, /docs-health showing your own rot, measured freshness instead of the claim real-time. The buyer already suspects the weaknesses; naming them first is the only cheap way to be believed about the strengths.
- (insight by coach) SYNTHESIS 5 - Support deflection has one mechanic, not many: name pages after the symptom the user types when broken (why is my lead list empty, why is the phone column blank, what does Enriching mean) and then wire the app's own empty states to those anchors. That turns docs from a place you visit into part of the product's error surface. Nine known causes of an empty lead list currently exist only as one-line asides on unrelated pages.
- (insight by coach) SYNTHESIS 6 - The hiring-events ledger is the only asset here that CREATES traffic rather than serving traffic that already arrived. Programmatic market pages (role x geo x month) generated from role-demand and repost-pain are unique, un-copyable, and each one can end with the exact search config that reproduces it - so the SEO page is also the product demo and the signup path.
- (insight by coach) SYNTHESIS 7 - Resolved contradiction: publish the affiliate commissions vs stop reselling your customers budget to eight affiliates collapses into a stronger third option - no affiliate link renders without its measured cost-per-usable-lead beside it, plus a ninth row for a SignalsAPI managed enrichment SKU. The liability becomes the ranking asset and the monetization at once.
- (insight by coach) SYNTHESIS 8 - /docs-health is not a page, it is the governance mechanism that answers the Five Whys root cause (nobody owns the docs, so every fact rots). It was independently invented 5 times. Paired with last_verified front matter and CI gates it makes regression visible by default - the only fix in the whole set that is structural rather than editorial.
- (insight by coach) SYNTHESIS 9 - Highest ratio of impact to effort in the entire session: delete one character. _config.yml line 7 url has a trailing slash, which malforms all 28 canonical URLs, og:urls and JSON-LD urls into docs.signalsapi.com//features/... Google indexes the broken form. One-character fix, sitewide SEO repair.
- (direction by coach) Recommended sequencing: (1) Monday credibility patch - trailing slash, exclude README/push.sh/run.sh, fix the broken ai-variables link, strip the dead phone pricing from faq.md, add sitemap/robots/404. (2) Core concepts + Getting started + a symptom-named troubleshooting section. (3) _data single-source-of-truth refactor with CI gates. (4) Public sandbox key + llms.txt + OpenAPI + docs MCP server. (5) Ledger-generated market pages.
- (event by coach) session complete - 81 verified audit findings, 311 ideas, 14 techniques, 9 synthesis insights
